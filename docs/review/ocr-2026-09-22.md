<!-- SPDX-FileCopyrightText: 2026 HanYang06 -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

# OpenCodeReview 评审 findings（2026-09-22）

来源：`alibaba/open-code-review`（模型 DeepSeek `deepseek-flash`）全库评审，共 **176 条**。
本文件从导出的 `review.html` 中拆出纯 findings（剔除会话 / 工具调用 / Token 统计等噪声），
做成可勾选的待办清单；解决一条勾一条。

- 总计 **176** 条；**已修 41**，待办 **135**。
- 已修条目：PR [#1](https://github.com/HanYang06/cairn/pull/1)（其中 6 条为顺手修的低影响项）。
- 分类 `severity / category` 为工具原始判定；`#编号` 是工具内序号，可回 `review.html` 对照。

## 待办分布

| 模块 | 待办 | 已修 |
|---|---:|---:|
| `core` | 46 | 9 |
| `feature` | 34 | 13 |
| `ui_tools` | 47 | 15 |
| `app` | 8 | 4 |

## 低影响项分诊（初判，逐条确认）

> 逐条核验后的粗分，用于决定「怎么修」。分类会随处理调整；同一编号可能从不同视角重复出现。

### A. 事实 / 健壮性 —— 建议采纳（改法不动设计语义）

输入校验与 fail-loud、错误类型语义、生命周期/并发健壮性、热点性能、文档文案。代表项：#25 空参数生成非法 SQL、#26 `= NULL` 永不命中、#48 `decode` 缺 id 自造身份、#71 裸字符串被逐字符拆行、#82/#83 样式脏数据崩溃、#88 `order` 未校验排列、#110 `int(created)` 截断使签名可绕过、#132/#133 配置形状校验、#139/#158 观察者异常隔离、#146/#142 越界索引、#162 主题悬空令牌静默、#171 网格 cols 非法、#173/#174 Show 容器/去重。

### B. 设计取舍 —— 保留，补文档说明理由

单连接无并发（#33）、`head()` 非热点（#36）、`#44` ULID 措辞、`#46` 未绑定直调（有测试锁定）、`#73` 直线不提供给 UI、`#77` 对齐 toggle 语义、`#79` 循环工具无高亮（设计）、`#81` `run_tool` 返回「已派发」、`#107` 组内 gid/oid 双身份、`#157` locked/addable、`#27` `Table` 提交归桶。

### C. 疑似过度设计 —— 建议单独拉出来：删 / 简化 / 重构

这批最值得警惕，说明抽象或机制本身可能多余：`#11` `self.feature` 死状态、`#14` `codec.py` 与 `block.canonical` 重复（疑似可删）、`#19`/`#20`/`#21`/`#22`/`#45`/`#46` 信号主干句柄身份与私有状态耦合、`#28` `INSERT OR REPLACE`、`#34` 版本 id 可撞、`#35` 保留窗永不触发（未接调用）、`#38` 可变默认、`#42` `ObjectInfo` 假 frozen、`#47` `Block.read` 契约、`#50` 哈希缓存可信、`#52` 类型登记半归一、`#55` 导航条目未接线、`#78` `SetStyleTool` 死代码、`#97` `List` 遮蔽 `typing.List`、`#99`/`#126` 浅拷贝、`#111` 签名点分编码有歧义、`#116` `asset.mime` 是 ClassVar 死值、`#119` `Bind._owner` 死状态、`#128` `_closed` 词表未校验、`#134`/`#135` 主题词表有不可达项、`#147` 硬编码兜底色、`#175` `Show` 半 dataclass。

**建议节奏**：A 类分批直接修（每批一个 PR）；B 类只补注释/文档；C 类先讨论「删还是留」，再单独开重构 PR，避免边修 bug 边扩散改动。

# 模块 `core`

## `src/core/codec.py`

待办 2 / 共 3

- [x] **#12** · `medium/bug` · `L18` · `canonical=True` makes cbor2 order the entries of every map, and cbor2 does that with a plain Python sort (`so… · ❌ 误报（无需改）
  <details><summary>详情与建议</summary>

  `canonical=True` makes cbor2 order the entries of every map, and cbor2 does that with a plain Python sort (`sorted(value.items())` / `PyList_Sort`) rather than a byte-wise sort of the encoded keys. A `dict` with heterogeneous keys (e.g. `{1: "a", "k": "b"}`) is perfectly valid CBOR but is not orderable in Python, so `encode_cbor` raises `TypeError: '<' not supported between instances of 'str' and 'int'` for input that this wrapper presents as generic. Either state the homogeneous/`str`-key restriction in the docstring, or convert the raw `TypeError` into an explicit, documented failure so callers get a meaningful error instead of an opaque comparison error.

  **现有代码**

  ```
  18    return cbor2.dumps(obj, canonical=True)
  ```

  **建议**

  ```
      try:
          return cbor2.dumps(obj, canonical=True)
      except TypeError as exc:  # canonical 排序要求 map 键同构（建议全部为 str）
          raise ValueError("canonical CBOR 编码失败：map 键类型不一致") from exc
  ```

  </details>
- [ ] **#13** · `medium/security` · `L23` · `decode_cbor` hands the payload straight to `cbor2.loads` with no input-type check, no size limit and no depth…
  <details><summary>详情与建议</summary>

  `decode_cbor` hands the payload straight to `cbor2.loads` with no input-type check, no size limit and no depth limit, and lets `cbor2.CBORDecodeError` escape. This module is documented as the cross-layer transport/hashing boundary, so it is exactly the place where malformed or hostile CBOR arrives: an oversized or deeply nested payload can exhaust memory/recursion, and a non-bytes argument produces a confusing error far from the call site. Note that `src/core/storage/block.py` already centralises this concern for its own decode path (it wraps decode errors into `CorruptObjectError`); this module should do the equivalent instead of leaving every caller to guard the crash path.

  **现有代码**

  ```
  23    return cbor2.loads(data)
  ```

  **建议**

  ```
      if not isinstance(data, (bytes, bytearray, memoryview)):
          raise TypeError("data must be bytes-like")
      if len(data) > _MAX_CBOR_BYTES:
          raise ValueError("CBOR payload exceeds the size limit")
      try:
          return cbor2.loads(data)
      except cbor2.CBORDecodeError as exc:
          raise ValueError("CBOR decode failed") from exc
  ```

  </details>
- [ ] **#14** · `low/maintainability` · `L26` · This module duplicates a wrapper that already exists in `src/core/storage/block.py` (`canonical()` / `decode_c…
  <details><summary>详情与建议</summary>

  This module duplicates a wrapper that already exists in `src/core/storage/block.py` (`canonical()` / `decode_canonical()` are byte-for-byte the same two one-liners), yet the names differ (`encode_cbor` vs `canonical`), and a repository-wide search shows neither `encode_cbor` nor `decode_cbor` is imported anywhere. Having two copies of the determinism-critical encoder makes it easy for the two to drift apart (e.g. only one of them gaining a key-sort or error-handling fix), which would silently break hash/dedup equality across layers. Please converge on a single implementation and have `block.py` import from here (or drop the unused copy).

  **现有代码**

  ```
  26__all__ = ["decode_cbor", "encode_cbor"]
  ```

  **建议**

  _（无）_

  </details>

## `src/core/conf/core.py`

待办 2 / 共 2

- [ ] **#2** · `medium/maintainability` · `L10` · The version token `v1` inside the context path is encoded independently from `FORMAT_VERSION = 1`.
  <details><summary>详情与建议</summary>

  The version token `v1` inside the context path is encoded independently from `FORMAT_VERSION = 1`. If the format version is bumped without also editing this string, the format version and the vault meta context path silently desynchronise, which can break lookups that rely on them agreeing. Derive the token from the constant so there is a single source of truth.

  **现有代码**

  ```
  10VAULT_META_CONTEXT = "cairn/v1/vault/meta"
  ```

  **建议**

  ```
  VAULT_META_CONTEXT = f"cairn/v{FORMAT_VERSION}/vault/meta"
  ```

  </details>
- [ ] **#3** · `low/documentation` · `L11` · The 30-day window is expressed as an unexplained multiplication chain.
  <details><summary>详情与建议</summary>

  The 30-day window is expressed as an unexplained multiplication chain. Since the `_MS` suffix implies milliseconds, a reader has to reverse-engineer the unit and the 30-day intent; anyone changing it in seconds (e.g. `30 * 24 * 60 * 60`) would silently shrink the window by 1000x. Add a short comment documenting the unit and the 30-day semantics.

  **现有代码**

  ```
  11VERSION_WINDOW_MS = 30 * 24 * 60 * 60 * 1000
  ```

  **建议**

  ```
  # Version validity window: 30 days, in milliseconds.
  VERSION_WINDOW_MS = 30 * 24 * 60 * 60 * 1000
  ```

  </details>

## `src/core/policy.py`

待办 2 / 共 2

- [ ] **#16** · `medium/bug` · `L52` · `parse_kind` claims to accept "any value", but `str()` on an already-parsed enum does not yield the enum's val…
  <details><summary>详情与建议</summary>

  `parse_kind` claims to accept "any value", but `str()` on an already-parsed enum does not yield the enum's value: `ShareKind` is a plain `Enum`, so `str(ShareKind.HOMEPAGE)` is `'ShareKind.HOMEPAGE'`, not `'homepage'`. Passing a valid `ShareKind` therefore raises `ValueError` and is silently reported as unrecognized, so any caller that round-trips a `ShareKind` back in (e.g. a share entry that stores the enum object rather than its string value) will silently lose the target. Handle the enum case explicitly (or make `ShareKind` inherit from `str`, e.g. `class ShareKind(str, Enum)`).

  **现有代码**

  ```
  52        return ShareKind(str(value))
  ```

  **建议**

  ```
          if isinstance(value, ShareKind):
              return value
          return ShareKind(str(value))
  ```

  </details>
- [ ] **#17** · `low/performance` · `L64` · `not list(shares)` builds a full list just to test emptiness.
  <details><summary>详情与建议</summary>

  `not list(shares)` builds a full list just to test emptiness. Since the parameter is declared as `Iterable`, callers may pass a generator; this needlessly allocates the entire collection (and drains the iterator) only to discard it. Prefer an existence check that short-circuits, or a plain truthiness check when a sized container is expected.

  **现有代码**

  ```
  64    return not list(shares)
  ```

  **建议**

  ```
      return not any(True for _ in shares)
  ```

  </details>

## `src/core/signal/bus.py`

待办 2 / 共 2

- [ ] **#19** · `medium/maintainability` · `L60-L61` · Delivery is gated on *object identity* of the `BoundTopic` handle.
  <details><summary>详情与建议</summary>

  Delivery is gated on *object identity* of the `BoundTopic` handle. This is correct only as an implicit, undocumented invariant: `BoundTopic` happens to define no `__eq__`, and `Topic.__get__` caches exactly one instance per domain instance, so `emit` and `subscribe` always see the same object. If a handle is ever rebuilt (a future non-caching access path, a transport/test that constructs `BoundTopic` directly, or adding value equality to `BoundTopic`), subscriptions would silently stop firing — no exception, no log — which is very hard to diagnose. Prefer matching on the topic definition/name (or a dict keyed by topic) so the contract does not depend on handle identity, or at minimum document the requirement in `subscribe`'s docstring.

  **现有代码**

  ```
  60            if isinstance(event, _TopicEvent) and event.topic is target:
  61                handler(event.data)
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#20** · `low/maintainability` · `L49` · `invoke` ignores `self` entirely and pokes Action's private state: any `Signal` can dispatch any `Action` thro…
  <details><summary>详情与建议</summary>

  `invoke` ignores `self` entirely and pokes Action's private state: any `Signal` can dispatch any `Action` through this public API, including an Action bound to a *different* bus, or one whose `_signal is None` (i.e. a domain that was never bound). That bypasses the unbound guard implemented in `Action.__call__` (`if self._signal is None: ...`) and in `Domain.bus`, so the "unbound domain cannot dispatch" contract is only enforced on the convenience path. Suggest adding a non-dispatching internal entry point on `Action` and validating ownership here, e.g. `if action._signal is not self: raise SignalError(...)` before delegating.

  **现有代码**

  ```
  49        return action._func(action._owner, *args, **kwargs)  # noqa: SLF001 — 动作句柄与总线同包强耦合
  ```

  **建议**

  _（无）_

  </details>

## `src/core/signal/events.py`

待办 3 / 共 3

- [ ] **#21** · `medium/bug` · `L50-L53` · `_Subscription` is a non-frozen dataclass, so `__eq__` is auto-generated and compares `(handler, event_type)`.
  <details><summary>详情与建议</summary>

  `_Subscription` is a non-frozen dataclass, so `__eq__` is auto-generated and compares `(handler, event_type)`. `_remove` then uses value-based matching (`inner in self._subscriptions` / `list.remove`), and handlers are often bound methods for which `a.m == a.m` is `True`. If the same callable subscribes twice for the same event type, `cancel()` on the second handle may remove the *first* entry instead of its own, leaving a handle that reports `_active == True` while its own entry (or an equivalent one) is already gone — the handle/entry identity silently diverges. Currently the two entries are interchangeable so the aggregate dispatch count stays correct, but the invariant is fragile: any future field added to `_Subscription`, or any callable with value-based equality (`functools.partial`, callable objects), turns this into a wrong-entry removal. Give `_Subscription` identity semantics explicitly (e.g. `@dataclass(slots=True, eq=False)`) or match by identity in `_remove`.

  **现有代码**

  ```
  50@dataclass(slots=True)
  51class _Subscription:
  52    handler: Handler
  53    event_type: type[Event]
  ```

  **建议**

  ```
  @dataclass(slots=True, eq=False)
  class _Subscription:
      handler: Handler
      event_type: type[Event]
  ```

  </details>
- [ ] **#22** · `low/bug` · `L64-L67` · `cancel()` performs an unsynchronized check-then-act on `_active`: the flag is read and written outside the bu…
  <details><summary>详情与建议</summary>

  `cancel()` performs an unsynchronized check-then-act on `_active`: the flag is read and written outside the bus lock, so two threads cancelling the same handle concurrently both pass the `if` and call `_remove`. This is only benign because `_remove` re-checks membership and is idempotent — the safety relies on that side effect rather than on an explicit guard. A stronger correctness concern is the interaction with `emit`: targets are snapshotted under the lock and handlers run outside it, so a subscription cancelled (from any thread, or by an earlier handler during the same dispatch) after the snapshot but before its turn still receives the event, contradicting the documented guarantee of this class ("`cancel()` 后不再收到事件"). If that guarantee is meant to hold strictly, re-check the subscription's active state under the lock right before invoking each handler.

  **现有代码**

  ```
  64    def cancel(self) -> None:
  65        if self._active:
  66            self._bus._remove(self._inner)  # noqa: SLF001 — 订阅句柄与总线同模块强耦合
  67            self._active = False
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#23** · `low/maintainability` · `L88-L91` · The bus stores a strong reference to `handler` (and thus to any bound object / closure it captures) for the wh…
  <details><summary>详情与建议</summary>

  The bus stores a strong reference to `handler` (and thus to any bound object / closure it captures) for the whole lifetime of the bus. If a caller discards the returned `Subscription` without calling `cancel()` (or using `with`), the handler is retained forever and keeps receiving events — an unbounded leak for a long-lived, process-wide bus. Consider documenting the required cleanup explicitly, or holding weak references to handlers if subscriptions are expected to be ad-hoc.

  **现有代码**

  ```
  88        inner = _Subscription(handler=handler, event_type=event_type)
  89        with self._lock:
  90            self._subscriptions.append(inner)
  91        return Subscription(self, inner)
  ```

  **建议**

  _（无）_

  </details>

## `src/core/signal/service.py`

待办 2 / 共 2

- [ ] **#45** · `medium/bug` · `L176-L179` · `BoundTopic` 会把**首次访问时**的 `obj._signal` 快照下来并永久缓存到实例 `__dict__`。
  <details><summary>详情与建议</summary>

  `BoundTopic` 会把**首次访问时**的 `obj._signal` 快照下来并永久缓存到实例 `__dict__`。若该域在 `bind()` 之前被访问（或在 `bind()` 之后被重绑到另一条总线），缓存句柄会一直指向 `None`/旧总线：此后 `emit` 静默丢弃、`subscribe` 恒抛 `SignalError`，即使再调用 `bind()` 也无法恢复。另外 `isinstance(cached, BoundTopic)` 恒为真（只会缓存该类对象），并不能兜住这类过期缓存。建议以当前 `obj._signal` 判断缓存是否仍然有效。

  **现有代码**

  ```
  176        cached = obj.__dict__.get(key)
  177        if isinstance(cached, BoundTopic):
  178            return cached
  179        bound = BoundTopic(obj._signal, self)  # noqa: SLF001 — 域与主干同包强耦合
  ```

  **建议**

  ```
          bound = obj.__dict__.get(key)
          if not isinstance(bound, BoundTopic) or bound._signal is not obj._signal:  # noqa: SLF001
              bound = BoundTopic(obj._signal, self)  # noqa: SLF001 — 域与主干同包强耦合
              obj.__dict__[key] = bound
          return bound
  ```

  </details>
- [ ] **#46** · `low/maintainability` · `L72-L74` · 未绑定总线时 `Action.__call__` 会**退回本地直调**，这与模块/类文档声明的“调用经总线派发”契约不一致，也与 `BoundTopic.subscribe` / `Domain.bus` 在未绑定时抛…
  <details><summary>详情与建议</summary>

  未绑定总线时 `Action.__call__` 会**退回本地直调**，这与模块/类文档声明的“调用经总线派发”契约不一致，也与 `BoundTopic.subscribe` / `Domain.bus` 在未绑定时抛错的行为不统一：域漏了 `bind()` 时不会报错，而是静默执行，日后总线若加入鉴权/埋点等逻辑也会被绕过。建议在未绑定时抛 `SignalError`（如确有测试需要本地直调，请显式说明并补注释）。

  **现有代码**

  ```
  72        if self._signal is None:
  73            return self._func(self._owner, *args, **kwargs)
  74        return self._signal.invoke(self, *args, **kwargs)
  ```

  **建议**

  ```
          if self._signal is None:
              raise SignalError(f"域未绑定总线：{type(self._owner).__name__}.{self._name}")
          return self._signal.invoke(self, *args, **kwargs)
  ```

  </details>

## `src/core/storage/__init__.py`

待办 1 / 共 1

- [ ] **#18** · `low/documentation` · `L11` · The module docstring states the layer has "只有两个概念" (only two concepts: Bucket and Block) and that everything e…
  <details><summary>详情与建议</summary>

  The module docstring states the layer has "只有两个概念" (only two concepts: Bucket and Block) and that everything else is a block `type`/`body`, but the package actually re-exports additional first-class concepts that are *not* block types — `Catalog`/`BlockLocation` (on-disk pack catalog), `Table` (SQL helper) and `VersionStore`/`Codec`/`version_id`/`RETENTION_MS` (version history, which `block.py` explicitly says blocks do not carry). This reads as contradictory for users reading the docstring as the public API contract. Consider rephrasing to describe Bucket/Block as the core data model while noting the auxiliary infrastructure exports.

  **现有代码**

  ```
  11其余一切（笔记 / 项目 / 多媒体 / 索引 / 变更）都是块的一种 ``type`` / ``body``。
  ```

  **建议**

  _（无）_

  </details>

## `src/core/storage/block.py`

待办 5 / 共 5

- [ ] **#47** · `high/bug` · `L429-L431` · `read()` blindly calls `bytes(body)` on any non-`bytes` body, but the class explicitly allows raw list/scalar …
  <details><summary>详情与建议</summary>

  `read()` blindly calls `bytes(body)` on any non-`bytes` body, but the class explicitly allows raw list/scalar bodies (`BodyField()` doc: 「裸 body（bytes / list / 标量）」) and structured `Body` bodies. For a list body this silently returns garbage (e.g. `bytes([1, 2, 3]) == b'\x01\x02\x03'`), for a `str`/`Body` body it raises an opaque `TypeError`. Since the declared contract is `-> bytes`, it should validate and fail loudly instead of returning meaningless data.

  **现有代码**

  ```
  429    def read(self) -> bytes:
  430        body = self.body
  431        return bytes(body) if not isinstance(body, bytes) else body
  ```

  **建议**

  ```
      def read(self) -> bytes:
          body = self.body
          if not isinstance(body, (bytes, bytearray)):
              raise CairnError(f"body 不是字节内容：{type(body).__name__}")
          return bytes(body)
  ```

  </details>
- [ ] **#50** · `medium/bug` · `L344-L345` · `body_hash()` is the dedup key used by `bucket.put` and the basis of `verify()`, yet it trusts a cached `body.…
  <details><summary>详情与建议</summary>

  `body_hash()` is the dedup key used by `bucket.put` and the basis of `verify()`, yet it trusts a cached `body.hash` and only recomputes when the cache is falsy. Any body mutated without calling `refresh()` (the convention documented on `Body` is unenforced) yields a checksum that no longer matches the stored content — the content is then deduped against the wrong payload, and `verify()` still returns `True` because it recomputes from the same stale value. Recomputing here (or invalidating on mutation) makes the cache self-consistent; NoteData already works around this by overriding `body_hash()` to always `refresh()`.

  **现有代码**

  ```
  344        if isinstance(body, Body):
  345            return body.hash or body.refresh().hash
  ```

  **建议**

  ```
          if isinstance(body, Body):
              return body.refresh().hash
  ```

  </details>
- [ ] **#48** · `medium/bug` · `L387` · `id` defaults to `None`, and `Block.__init__` turns a `None` id into a brand-new Oid (`id if id is not None el…
  <details><summary>详情与建议</summary>

  `id` defaults to `None`, and `Block.__init__` turns a `None` id into a brand-new Oid (`id if id is not None else str(Oid.new())`). So `Block.decode(payload)` without an explicit id silently invents a fresh identity for a record that was read back from storage. Because `checksum` only covers the body, `verify()` still passes, so the mis-association is invisible. Require an id here (the bucket already always passes one).

  **现有代码**

  ```
  387            block = target(id=id, body=body, attrs=dict(attrs or {}), type=kind)
  ```

  **建议**

  ```
              if id is None:
                  raise CorruptObjectError("decode 需要块 id")
              block = target(id=id, body=body, attrs=dict(attrs or {}), type=kind)
  ```

  </details>
- [ ] **#49** · `medium/bug` · `L389-L396` · The `try` block wraps far more than the CBOR decode: subclass construction, `_body_from_data` (`from_data` of …
  <details><summary>详情与建议</summary>

  The `try` block wraps far more than the CBOR decode: subclass construction, `_body_from_data` (`from_data` of arbitrary domain classes) and `compute_checksum` are all inside it, and `TypeError`/`ValueError` are caught broadly. A programming error in a domain `from_data` (or in `content()`/`canonical`) is thus relabelled as `CorruptObjectError("块解析失败")`, hiding the real cause while a corrupt payload can still escape as an unexpected exception type. Narrow the guard to the decoding/coercion step and let programming errors propagate.

  **现有代码**

  ```
  389        except (
  390            cbor2.CBORDecodeError,
  391            cbor2.CBOREncodeError,
  392            KeyError,
  393            TypeError,
  394            ValueError,
  395        ) as exc:
  396            raise CorruptObjectError("块解析失败") from exc
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#51** · `low/maintainability` · `L260-L261` · `_is_attr_annotation` deliberately matches both `Attr` and `Data`, but the branch below always builds an `Attr…
  <details><summary>详情与建议</summary>

  `_is_attr_annotation` deliberately matches both `Attr` and `Data`, but the branch below always builds an `Attr` via `_attr_from_value(value)`. Consequently a `Data[...]`-annotated field declared with a bare default (e.g. `canvas: Data = ...`) silently becomes an `Attr` descriptor, losing the data-field distinction that the file otherwise maintains through `_data_from_value` (`Data(factory=...)`). Route `Data` annotations to `Data(...)` (or narrow `_is_attr_annotation` to `Attr` only) so declared semantics survive.

  **现有代码**

  ```
  260            elif _is_attr_annotation(annotation):
  261                descriptor = _attr_from_value(value)
  ```

  **建议**

  _（无）_

  </details>

## `src/core/storage/bucket.py`

待办 4 / 共 7

- [x] **#57** · `high/bug` · `L280-L281` · Integrity check is a no-op. ✅
  <details><summary>详情与建议</summary>

  Integrity check is a no-op. `Block.decode()` sets `block.checksum = block.compute_checksum()` right before returning (block.py), so `block.verify()` — which just compares `checksum` with `compute_checksum()` — is always `True`. A payload that was tampered with or corrupted inside the pack file is therefore returned as a valid block. Compare the recomputed hash against the checksum actually recorded in the catalog (`row["body_id"]`), which is the value the location was looked up by.

  **现有代码**

  ```
  280        if not block.verify():
  281            raise CorruptObjectError(f"块校验失败: {block_id}")
  ```

  **建议**

  ```
          if block.compute_checksum() != checksum:
              raise CorruptObjectError(f"块校验失败: {block_id}")
  ```

  </details>
- [ ] **#59** · `medium/bug` · `L131-L135` · Nested transactions are not really atomic.
  <details><summary>详情与建议</summary>

  Nested transactions are not really atomic. When an inner `transaction()` fails at depth > 0, only `_tx_depth` is decremented and no rollback/SAVEPOINT is issued; if the caller catches that exception inside the outer block (a common pattern: `with bucket.transaction(): ... try: with bucket.transaction(): ... except ...`), execution continues and the outer transaction commits the partially written work, contradicting the documented "块与业务表一起提交或一起回滚". Either mark the transaction as poisoned so the outermost scope rolls back (see suggestion, requires initialising `self._tx_failed = False` in `__init__`), or use SQLite SAVEPOINTs per nesting level.

  **现有代码**

  ```
  131        except BaseException:
  132            self._tx_depth -= 1
  133            if self._tx_depth == 0:
  134                self.catalog.conn.rollback()
  135            raise
  ```

  **建议**

  ```
          except BaseException:
              self._tx_depth -= 1
              self._tx_failed = True
              if self._tx_depth == 0:
                  self._tx_failed = False
                  self.catalog.conn.rollback()
              raise
  ```

  </details>
- [x] **#58** · `medium/bug` · `L271-L272` · Corrupt block metadata is silently downgraded to `{}` instead of being reported: when the decoded `data` blob … ✅
  <details><summary>详情与建议</summary>

  Corrupt block metadata is silently downgraded to `{}` instead of being reported: when the decoded `data` blob is not a dict (e.g. a list/int/str left by a partial write or corruption), `params` becomes empty, so `attrs`, `author` and `config` are dropped, the object is returned as valid and a later `put` will permanently overwrite the original metadata. Additionally, a CBOR decode failure raises `cbor2.CBORDecodeError`, which is not a `CairnError`, so callers that catch `CairnError`/`CorruptObjectError` will not handle it. Raise `CorruptObjectError` for both cases.

  **现有代码**

  ```
  271        raw = decode_canonical(bytes(row["data"])) if row["data"] else {}
  272        params = raw if isinstance(raw, dict) else {}
  ```

  **建议**

  ```
          try:
              raw = decode_canonical(bytes(row["data"])) if row["data"] else {}
          except Exception as exc:  # cbor2.CBORDecodeError
              raise CorruptObjectError(f"块元数据解析失败: {block_id}") from exc
          if not isinstance(raw, dict):
              raise CorruptObjectError(f"块元数据非法: {block_id}")
          params = raw
  ```

  </details>
- [ ] **#61** · `medium/bug` · `L310-L313` · Corrupt or schema-mismatched stored config is silently swallowed and replaced by defaults.
  <details><summary>详情与建议</summary>

  Corrupt or schema-mismatched stored config is silently swallowed and replaced by defaults. This hides a damaged or partially migrated bucket (e.g. a config written by an older/newer format with unknown keys) and silently changes `block_max_bytes`/`pack_max_*`, which directly alters how existing data is chunked and when packs are sealed. Surface it instead of guessing.

  **现有代码**

  ```
  310        try:
  311            return BucketConfig(**json.loads(raw))
  312        except (TypeError, ValueError):
  313            return BucketConfig()
  ```

  **建议**

  ```
          try:
              return BucketConfig(**json.loads(raw))
          except (TypeError, ValueError) as exc:
              raise CairnError(f"桶配置损坏: {raw!r}") from exc
  ```

  </details>
- [x] **#60** · `medium/bug` · `L340-L344` · The bytes are appended and `fsync`ed before the enclosing catalog transaction commits. ✅
  <details><summary>详情与建议</summary>

  The bytes are appended and `fsync`ed before the enclosing catalog transaction commits. If that transaction later rolls back (e.g. the caller's `transaction()` block raises), `grow_pack()` accounting is reverted but the data stays in the pack file, so `packs.blocks`/`packs.bytes` permanently undercount the file. Consequences: `_active_pack()` no longer seals the pack when it really reached `pack_max_bytes`/`pack_max_blocks` (limits can be exceeded), and stray pack files created by a rolled-back `_new_pack()` are left behind because `catalog.pack_name()` will no longer find their row. Track the pre-append offset/size and compensate (truncate the pack file) when the write or the transaction fails.

  **现有代码**

  ```
  340        with path.open("ab") as handle:
  341            offset = handle.tell()
  342            handle.write(payload)
  343            handle.flush()
  344            os.fsync(handle.fileno())
  ```

  **建议**

  ```
          with path.open("ab") as handle:
              offset = handle.tell()
              try:
                  handle.write(payload)
                  handle.flush()
                  os.fsync(handle.fileno())
              except BaseException:
                  handle.truncate(offset)  # 失败即回退已写字节，保持文件与目录一致
                  raise
  ```

  </details>
- [ ] **#62** · `low/bug` · `L224-L227` · `put_content()` is not atomic: each `put()` commits (and fsyncs) on its own and the index block is written las…
  <details><summary>详情与建议</summary>

  `put_content()` is not atomic: each `put()` commits (and fsyncs) on its own and the index block is written last, so a failure half-way leaves unreferenced `part` blocks plus their bodies in the catalog and pack bytes on disk that compaction cannot tie to any live object. Wrapping the whole sharded write in `with self.transaction():` makes the parts+index a single commit and also removes the per-part commit/fsync overhead.

  **现有代码**

  ```
  224        parts = [
  225            self.put(Block(body=data[start : start + step], type=PART_TYPE)).id
  226            for start in range(0, len(data), step)
  227        ]
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#63** · `low/maintainability` · `L273` · This local variable shadows the module-level `type_name` imported from `core.types` and used by `_save_block`.
  <details><summary>详情与建议</summary>

  This local variable shadows the module-level `type_name` imported from `core.types` and used by `_save_block`. It works today because `_get` never calls the function afterwards, but any future use of `type_name(...)` inside this method will fail confusingly. Rename the local (e.g. `stored_type`) to keep the import usable.

  **现有代码**

  ```
  273        type_name = self.catalog.type_name(int(row["type"]))
  ```

  **建议**

  ```
          stored_type = self.catalog.type_name(int(row["type"]))
  ```

  </details>

## `src/core/storage/catalog.py`

待办 3 / 共 4

- [ ] **#29** · `high/security` · `L244` · `create_table` builds DDL by interpolating the table name, column names and column specs directly into an f-st…
  <details><summary>详情与建议</summary>

  `create_table` builds DDL by interpolating the table name, column names and column specs directly into an f-string; SQL identifiers cannot be bound as parameters, so any caller-supplied value containing quotes/`);` (e.g. `bucket.table("x); DROP TABLE block; --")`) yields arbitrary SQL execution. This is reachable from the public `Bucket.table(name, **columns)` API, so validate every identifier against a strict whitelist before interpolating (reject rather than silently escape).

  **现有代码**

  ```
  244        self.conn.execute(f'CREATE TABLE IF NOT EXISTS "{name}" ({", ".join(parts)})')
  ```

  **建议**

  ```
  _IDENT_RE = re.compile(r"^[A-Za-z_][A-Za-z0-9_]*$")
  
      def create_table(self, name: str, columns: dict[str, str]) -> None:
          if not _IDENT_RE.match(name) or not all(_IDENT_RE.match(c) for c in columns):
              raise CairnError(f"非法表名或列名: {name}")
          parts = []
          for column, spec in columns.items():
              parts.append(f'"{column}" {spec}'.strip())
          self.conn.execute(f'CREATE TABLE IF NOT EXISTS "{name}" ({", ".join(parts)})')
  ```

  </details>
- [x] **#30** · `medium/bug` · `L91-L94` · The existing `catalog_version` is never read before being overwritten. ✅
  <details><summary>详情与建议</summary>

  The existing `catalog_version` is never read before being overwritten. Opening a catalog written by a *newer* version silently rewrites the marker to 2 and then runs queries against a schema it does not understand (potential data corruption), and the downgrade also hides the incompatibility from later readers. Read the stored version first, fail closed when it is greater than `CATALOG_VERSION`, and only write the version when creating a brand-new catalog (or after an actual migration).

  **现有代码**

  ```
  91        self.conn.execute(
  92            "INSERT OR REPLACE INTO meta(key, value) VALUES('catalog_version', ?)",
  93            (str(CATALOG_VERSION),),
  94        )
  ```

  **建议**

  ```
          row = self.conn.execute(
              "SELECT value FROM meta WHERE key = 'catalog_version'"
          ).fetchone()
          if row is not None and int(row["value"]) > CATALOG_VERSION:
              raise CairnError(
                  f"目录版本过新: {row['value']} > {CATALOG_VERSION}，请升级程序"
              )
          if row is None:
              self.conn.execute(
                  "INSERT INTO meta(key, value) VALUES('catalog_version', ?)",
                  (str(CATALOG_VERSION),),
              )
  ```

  </details>
- [ ] **#28** · `low/maintainability` · `L203` · `INSERT OR REPLACE` performs DELETE + INSERT, so the row is recreated rather than updated in place: any column…
  <details><summary>详情与建议</summary>

  `INSERT OR REPLACE` performs DELETE + INSERT, so the row is recreated rather than updated in place: any column not supplied here is reset, `rowid` may change, and once FK/cascade constraints are enabled (or other tables reference `block` by rowid) dependent rows would be deleted. Prefer an explicit upsert (`INSERT ... ON CONFLICT(oid) DO UPDATE SET ...`) so the update stays non-destructive.

  **现有代码**

  ```
  203            "INSERT OR REPLACE INTO block(oid, body_id, type, size, data, created, updated)"
  ```

  **建议**

  ```
              "INSERT INTO block(oid, body_id, type, size, data, created, updated)"
              " VALUES(?, ?, ?, ?, ?, ?, ?)"
              " ON CONFLICT(oid) DO UPDATE SET body_id=excluded.body_id, type=excluded.type,"
              " size=excluded.size, data=excluded.data, updated=excluded.updated"
  ```

  </details>
- [ ] **#31** · `low/performance` · `L229-L231` · `fetchall()` materializes every oid into a list that is then copied again by the list comprehension, holding t…
  <details><summary>详情与建议</summary>

  `fetchall()` materializes every oid into a list that is then copied again by the list comprehension, holding the entire block table in memory; callers only iterate. Stream rows from the cursor instead (and widen the return annotation / adjust `Bucket.iter_block_ids`, which already wraps this in a generator).

  **现有代码**

  ```
  229    def iter_block_ids(self) -> list[str]:
  230        rows = self.conn.execute("SELECT oid FROM block ORDER BY oid").fetchall()
  231        return [str(row["oid"]) for row in rows]
  ```

  **建议**

  ```
      def iter_block_ids(self) -> Iterator[str]:
          cursor = self.conn.execute("SELECT oid FROM block ORDER BY oid")
          for row in cursor:
              yield str(row["oid"])
  ```

  </details>

## `src/core/storage/table.py`

待办 3 / 共 4

- [x] **#24** · `high/bug` · `L44-L48` · `conflict` is accepted but discarded (`_ = conflict`), so a caller passing a non-default conflict column (e.g. ✅
  <details><summary>详情与建议</summary>

  `conflict` is accepted but discarded (`_ = conflict`), so a caller passing a non-default conflict column (e.g. `upsert(row, conflict="k")`) silently still conflicts on `id`. On top of that, `INSERT OR REPLACE` is a DELETE+INSERT, not an UPDATE: omitted columns are reset to their defaults and the rowid changes (breaking foreign keys/triggers). Implement a real upsert driven by `conflict`, or remove the unused parameter.

  **现有代码**

  ```
  44        self.conn.execute(
  45            f"INSERT OR REPLACE INTO {_quote(self.name)} ({names}) VALUES({placeholders})",
  46            [row[column] for column in columns],
  47        )
  48        _ = conflict
  ```

  **建议**

  ```
          if not columns:
              raise ValueError("upsert 需要至少一列")
          if conflict not in columns:
              raise ValueError(f"conflict 列不在行中: {conflict}")
          updates = ", ".join(f"{_quote(c)} = excluded.{_quote(c)}" for c in columns if c != conflict)
          self.conn.execute(
              f"INSERT INTO {_quote(self.name)} ({names}) VALUES({placeholders}) "
              f"ON CONFLICT({_quote(conflict)}) DO UPDATE SET {updates}",
              [row[column] for column in columns],
          )
  ```

  </details>
- [ ] **#27** · `medium/maintainability` · `L28-L29` · `Table` shares the `Catalog` connection but never commits, so every write here remains in the connection's imp…
  <details><summary>详情与建议</summary>

  `Table` shares the `Catalog` connection but never commits, so every write here remains in the connection's implicit transaction until a caller invokes `Bucket.commit()`/`Catalog.commit()`. The module docstring promises callers need not hand-write connections or transactions, yet a forgotten commit silently loses writes. Make this ownership explicit — document that the owning `Bucket` must commit, or provide a transaction boundary on `Table`.

  **现有代码**

  ```
  28        self.conn = conn
  29        self.name = name
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#26** · `medium/bug` · `L53` · Predicates are always generated as `col = ?`.
  <details><summary>详情与建议</summary>

  Predicates are always generated as `col = ?`. Passing `None` for a nullable column yields `col = NULL`, which is never true in SQL, so `select(col=None)`, `update(..., col=None)` and `delete(col=None)` silently match zero rows. Emit `IS NULL` / `IS NOT NULL` when the value is `None`.

  **现有代码**

  ```
  53            clause = " WHERE " + " AND ".join(f"{_quote(key)} = ?" for key in where)
  ```

  **建议**

  ```
              clause = " WHERE " + " AND ".join(
                  f"{_quote(key)} IS NULL" if value is None else f"{_quote(key)} = ?"
                  for key, value in where.items()
              )
  ```

  </details>
- [ ] **#25** · `low/bug` · `L31-L32` · Several `Table` helpers build SQL without validating their arguments, so empty input produces invalid SQL inst…
  <details><summary>详情与建议</summary>

  Several `Table` helpers build SQL without validating their arguments, so empty input produces invalid SQL instead of a clear error:
- `insert({})` builds `INSERT INTO "t" () VALUES()` (syntax error);
- `update({}, id=1)` expands to `SET  WHERE id = ?` and `update(values)` with no keyword filters expands to a trailing `WHERE `;
- `delete()` with no keyword arguments produces `DELETE FROM "t" WHERE `.
Validate that values/assignments are non-empty and that at least one filter is provided (or deliberately omit the `WHERE` clause for `delete`), raising a descriptive `ValueError` instead of surfacing an opaque syntax error.

  **现有代码**

  ```
  31    def insert(self, row: Mapping[str, Any]) -> None:
  32        columns = list(row)
  ```

  **建议**

  ```
      def insert(self, row: Mapping[str, Any]) -> None:
          columns = list(row)
          if not columns:
              raise ValueError("insert 需要至少一列")
  ```

  </details>

## `src/core/storage/version.py`

待办 5 / 共 6

- [x] **#32** · `high/bug` · `L146-L148` · `compact()` 按时间戳删除，而链的顺序由 `prev` 决定（模块文档明确「顺序不依赖时间」），`at` 又是调用方传入、并不保证沿链单调。 ✅
  <details><summary>详情与建议</summary>

  `compact()` 按时间戳删除，而链的顺序由 `prev` 决定（模块文档明确「顺序不依赖时间」），`at` 又是调用方传入、并不保证沿链单调。若中间某节点的 `at` 早于其祖先，它会被删掉，导致其祖先从 head 不可达：`head()` 会把这条孤儿节点当成新的 head（它已不被任何 `prev` 指向），`history()`/`state_at()` 会回放到错误的状态。此外逐行删除也不保证是连续的链尾。建议从 head 沿 `prev` 正向遍历，只删除「最早的连续一段」（即链尾一段），并在删除前先确认这些都不可达或直接由遍历结果决定。

  **现有代码**

  ```
  146        for row in self._table.select(oid=str(oid)):
  147            if int(row["at"]) < cutoff:
  148                self._table.delete(id=str(row["id"]))
  ```

  **建议**

  ```
          # 沿 prev 从 head 走，只丢最早的连续一段（链尾），避免把中间节点挖空
          entries = self.history(oid)  # 最新在前
          for entry in entries:
              if int(entry["at"]) >= cutoff:
                  continue
              # 只允许裁掉仍可自 head 触及的尾部节点
              self._table.delete(id=entry["id"])
              removed += 1
  ```

  </details>
- [ ] **#36** · `medium/performance` · `L102-L105` · `head()` 每次都把该 oid 的全部行拉出来重建 referenced 集合，而它被 `root()`、`commit()`、`history()` 反复调用；
  <details><summary>详情与建议</summary>

  `head()` 每次都把该 oid 的全部行拉出来重建 referenced 集合，而它被 `root()`、`commit()`、`history()` 反复调用；`history()` 自己还再做一次全量 select。链越长，单次写入/读取的代价越大。若这里确实在交互热路径上，建议改为「从 head 视角按需查询」（例如用一个 `prev IS NULL` 段的反向查询，或缓存 head），至少避免在同一次调用中重复全量扫描。

  **现有代码**

  ```
  102        rows = self._table.select(oid=str(oid))
  103        if not rows:
  104            return None
  105        referenced = {str(row["prev"]) for row in rows if row["prev"]}
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#33** · `medium/bug` · `L67-L68` · `root()` 与 `commit()` 都是「先 select 读 head，再 insert」的 check-then-act（commit 中的 `prev = self.head(oid)` 同理）。
  <details><summary>详情与建议</summary>

  `root()` 与 `commit()` 都是「先 select 读 head，再 insert」的 check-then-act（commit 中的 `prev = self.head(oid)` 同理）。若存在并发/重入的写入（同一 bucket 被多个 writer 使用），两个 writer 可能同时观察到无 head：`root()` 会插入两条 prev 为 NULL 的起点，`commit()` 会产出两个分支，破坏「单亲链」不变量，并使 `head()` 在多候选时返回不确定结果。建议在表上增加唯一约束（如 `(oid, prev)` 或对 `prev IS NULL` 的唯一索引）并捕获冲突，或在 entry 级别加锁串行化写路径。

  **现有代码**

  ```
  67        if self.head(oid) is not None:
  68            return None
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#34** · `medium/bug` · `L92-L95` · 版本 id 只由 `{prev, at, sig}` 决定，因此「同一 `prev`、同一毫秒、同一 `new_state`」的两次 `commit()` 会产生完全相同的 id（payload 也相同）。
  <details><summary>详情与建议</summary>

  版本 id 只由 `{prev, at, sig}` 决定，因此「同一 `prev`、同一毫秒、同一 `new_state`」的两次 `commit()` 会产生完全相同的 id（payload 也相同）。此时第二次 `insert` 会撞 PRIMARY KEY 抛 IntegrityError，而不是被当成幂等空操作（例如重试、事件被重复分发时就会触发）。建议插入时用 upsert/IGNORE 语义，或捕获冲突后返回已有 id。

  **现有代码**

  ```
  92        vid = version_id(prev, moment, codec.digest(new_state))
  93        self._table.insert(
  94            {"id": vid, "oid": str(oid), "prev": prev, "at": moment, "payload": payload}
  95        )
  ```

  **建议**

  ```
          vid = version_id(prev, moment, codec.digest(new_state))
          # 幂等：同一 (prev, at, sig) 重复提交时视为已存在，不再插入
          self._table.insert(
              {"id": vid, "oid": str(oid), "prev": prev, "at": moment, "payload": payload},
              on_conflict="ignore",
          )
  ```

  </details>
- [ ] **#35** · `medium/maintainability` · `L96-L99` · 模块文档承诺「保留窗默认 30 天，更新时惰性压实」，但 `root()`/`commit()` 都没有触发 `compact()`，仓库中只有测试直接调用它（`tests/feature/test_note_versi…
  <details><summary>详情与建议</summary>

  模块文档承诺「保留窗默认 30 天，更新时惰性压实」，但 `root()`/`commit()` 都没有触发 `compact()`，仓库中只有测试直接调用它（`tests/feature/test_note_versions.py`）。也就是说除非外部显式调用，保留窗永远不会生效。建议在 write 路径（例如 `commit()` 成功后）按 `RETENTION_MS` 触发一次惰性压实，或修正文档说明压实由调用方负责。

  **现有代码**

  ```
  96        self._bucket.commit()
  97        return vid
  98
  99    # ---- 读 ----
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#37** · `low/performance` · `L137` · 回放时每个节点都单独发一次 `patch()` select，形成 N+1 查询（配合 `history()` 内部的全量 select，长链下是 O(N²) 级别的 DB 往返）。
  <details><summary>详情与建议</summary>

  回放时每个节点都单独发一次 `patch()` select，形成 N+1 查询（配合 `history()` 内部的全量 select，长链下是 O(N²) 级别的 DB 往返）。可以一次性把这条链所有 payload 取回内存后再回放，减少往返次数。

  **现有代码**

  ```
  137            state = codec.apply(state, self.patch(oid, entry["id"]))
  ```

  **建议**

  _（无）_

  </details>

## `src/core/types/attr.py`

待办 4 / 共 4

- [ ] **#38** · `high/bug` · `L50` · `_initial()` 直接把 `self._default` 原样返回并写入 `obj.attrs[self.key]`。
  <details><summary>详情与建议</summary>

  `_initial()` 直接把 `self._default` 原样返回并写入 `obj.attrs[self.key]`。调用方只要传可变默认值（`Attr(default=[])` / `Attr(default={})`），这个对象就是**类级共享**的：所有实例的首次读取都会把同一个 list/dict 存进各自 `attrs`，任一个实例的原地修改（`append` / `[k]=v`）会污染其它实例乃至后续新建实例。这与 `_attr_from_value` 用 `factory=lambda: list(items)` 拷贝的既有约定不一致。建议对可变默认值做一份拷贝，或在 `__init__` 里直接拒绝可变 `default`（强制用 `factory`）。

  **现有代码**

  ```
  50        raw = None if self._default is _MISSING else self._default
  ```

  **建议**

  ```
          raw = None if self._default is _MISSING else copy.deepcopy(self._default)
  ```

  </details>
- [ ] **#39** · `medium/bug` · `L52-L53` · `item` 字段的默认值编码不对称：`__set__` 会对 list/tuple 的**每个元素**调 `_encode`，而 `_initial` 只在默认值本身带 `to_data` 时才编码。
  <details><summary>详情与建议</summary>

  `item` 字段的默认值编码不对称：`__set__` 会对 list/tuple 的**每个元素**调 `_encode`，而 `_initial` 只在默认值本身带 `to_data` 时才编码。因此 `Attr(item=X, default=[X(), X()])`（或 `factory` 返回一组类型化对象）会把**类型化对象**落进 `attrs`，而通过赋值写入的是紧凑数据形态，同一个字段的存储形态取决于写入路径。文档承诺「存储里是紧凑数据（dict）」，直接读 `attrs` / `meta()` / 落盘（CBOR）的代码会拿到不可序列化的对象。建议 `_initial` 对 list/tuple 逐元素编码，并让 `factory` 的返回值也走同一归一化。

  **现有代码**

  ```
  52        if self._item is not None and raw is not None and hasattr(raw, "to_data"):
  53            return self._encode(raw)
  ```

  **建议**

  ```
          if self._item is not None and raw is not None:
              if isinstance(raw, (list, tuple)):
                  return [self._encode(entry) for entry in raw]
              if hasattr(raw, "to_data"):
                  return self._encode(raw)
  ```

  </details>
- [ ] **#40** · `medium/bug` · `L59-L61` · `_decode` 的兜底分支假定存储值一定是 Mapping：当无 `from_data` 的元素类型拿到标量 / None / 字符串等非映射数据时，`self._item(**value)` 会抛出含义不明的 `T…
  <details><summary>详情与建议</summary>

  `_decode` 的兜底分支假定存储值一定是 Mapping：当无 `from_data` 的元素类型拿到标量 / None / 字符串等非映射数据时，`self._item(**value)` 会抛出含义不明的 `TypeError`（且 `isinstance(value, self._item)` 在 `_item` 不是类而是下标泛型／联合类型时同样会抛 `TypeError`）。建议显式校验并给出定位信息，便于排查脏数据。

  **现有代码**

  ```
  59        if hasattr(self._item, "from_data"):
  60            return self._item.from_data(value)
  61        return self._item(**value)
  ```

  **建议**

  ```
          if hasattr(self._item, "from_data"):
              return self._item.from_data(value)
          if not isinstance(value, Mapping):
              raise TypeError(f"字段 {self.key!r} 期望映射形态数据，得到 {type(value).__name__}")
          return self._item(**value)
  ```

  </details>
- [ ] **#41** · `low/maintainability` · `L77` · `__get__` 依据**运行时值的形态**（list/tuple vs Mapping）决定是「列表」还是「单个类型化值」，而不是依据字段声明的形态。
  <details><summary>详情与建议</summary>

  `__get__` 依据**运行时值的形态**（list/tuple vs Mapping）决定是「列表」还是「单个类型化值」，而不是依据字段声明的形态。后果：list 字段里若出现单个 Mapping（脏数据 / 旧格式），读取会静默返回一个对象而非列表；单个 item 字段若数据形态恰为 list（如 `to_data()` 返回数组），则会被逐元素解码成列表。建议在声明处记录形态（如显式的 `many`/`as_list` 标志）或至少用声明信息做校验，避免静默返回错误结构。

  **现有代码**

  ```
  77                return [self._decode(entry) for entry in value]
  ```

  **建议**

  _（无）_

  </details>

## `src/core/types/ids.py`

待办 2 / 共 2

- [ ] **#43** · `medium/bug` · `L55-L56` · `Cid.from_digest` hex-encodes whatever byte string it is given, so a digest that is not 32 bytes yields a `Cid…
  <details><summary>详情与建议</summary>

  `Cid.from_digest` hex-encodes whatever byte string it is given, so a digest that is not 32 bytes yields a `Cid` whose length is not the 64 characters that `Cid.parse` enforces. The two constructors therefore disagree on the class invariant, and any downstream code that trusts "a Cid is 64 lowercase hex chars" (hashing, comparison, storage keys) silently breaks on a short/long digest. Validate the digest length here.

  **现有代码**

  ```
  55    def from_digest(cls, digest: bytes) -> Cid:
  56        return cls(digest.hex())
  ```

  **建议**

  ```
      _DIGEST_BYTES = 32
  
      @classmethod
      def from_digest(cls, digest: bytes) -> Cid:
          if len(digest) != cls._DIGEST_BYTES:
              raise InvalidIdError(f"非法摘要长度: {len(digest)}")
          return cls(digest.hex())
  ```

  </details>
- [ ] **#44** · `low/documentation` · `L28` · The docstring claims the OID is "时间有序" (time-ordered), but only the millisecond timestamp prefix is ordered: t…
  <details><summary>详情与建议</summary>

  The docstring claims the OID is "时间有序" (time-ordered), but only the millisecond timestamp prefix is ordered: the 80-bit suffix is purely random, so two Oids minted in the same millisecond do not sort in generation order. Consider wording this as "ordered by generation time at millisecond granularity" so callers do not assume strict monotonic ordering (a monotonic ULID would need a counter for ties).

  **现有代码**

  ```
  28    """对象身份：ULID（26 字符 Crockford Base32，时间有序）。"""
  ```

  **建议**

  _（无）_

  </details>

## `src/core/types/kind.py`

待办 3 / 共 3

- [ ] **#52** · `medium/bug` · `L56` · `register` normalizes only the *lookup key* (`type_name(info.type)`) but stores `info` untouched, so `info.typ…
  <details><summary>详情与建议</summary>

  `register` normalizes only the *lookup key* (`type_name(info.type)`) but stores `info` untouched, so `info.type` / `info.units` / `info.deps` keep whatever the caller passed. The module's own contract is that enum and string are interchangeable (`type_info`, `unit_infos`, `domain_of` all normalize their lookups; the docstrings advertise value-normalization), so a caller registering an `Enum`-valued type/unit passes the registration silently, yet:
- `domain_of` compares its normalized `key` against the raw `info.units` and never matches (reverse lookup silently returns `None`);
- `types()` sorts on raw `info.type`, which raises `TypeError` as soon as a `str` and an `Enum` are mixed in one key tuple.
Suggest normalizing the stored values here (or asserting the invariant) instead of relying on every caller to pre-normalize.

  **现有代码**

  ```
  56    _TYPES[(type_name(info.type), info.role)] = info
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#54** · `low/maintainability` · `L89` · A unit name that was never registered (e.g.
  <details><summary>详情与建议</summary>

  A unit name that was never registered (e.g. a typo in a domain's `light` declaration) is silently skipped, so `unit_infos` returns a partial list and downstream UI just shows fewer fields with no diagnostic. Raise or at least log when `type_info` returns `None`, so a broken `units` declaration fails loudly.

  **现有代码**

  ```
  89        info = type_info(name, role=ROLE_DATA) or type_info(name)
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#53** · `low/maintainability` · `L98` · When several domains list the same data type in `units` (perfectly legal, e.g.
  <details><summary>详情与建议</summary>

  When several domains list the same data type in `units` (perfectly legal, e.g. a shared data type declared via `light=(...)`), this loop silently returns whichever domain happens to sort first in `types()`; the caller has no way to tell the owner is ambiguous. Consider detecting/logging the conflict, or documenting that the result is the first match in `(type, role)` order.

  **现有代码**

  ```
  98    for info in types(ROLE_DOMAIN):
  ```

  **建议**

  _（无）_

  </details>

## `src/core/types/objects.py`

待办 1 / 共 1

- [ ] **#42** · `medium/bug` · `L26` · `tags: dict[str, Any]` combined with `@dataclass(frozen=True, slots=True)` is inconsistent: with `eq=True` the…
  <details><summary>详情与建议</summary>

  `tags: dict[str, Any]` combined with `@dataclass(frozen=True, slots=True)` is inconsistent: with `eq=True` the frozen dataclass auto-generates `__hash__` over all fields, so `hash(ObjectInfo(...))` (e.g. putting an instance in a `set` / using it as a dict key) raises `TypeError: unhashable type: 'dict'`. The instance is also not truly immutable — the dict can still be mutated in place by any caller (`info.tags["x"] = 1`), silently changing a "frozen" object. Consider excluding the field from hashing/equality (`compare=False`) and/or storing an immutable mapping (e.g. `MappingProxyType`, or a `tuple[tuple[str, Any], ...]`) so the immutability contract actually holds.

  **现有代码**

  ```
  26    tags: dict[str, Any] = field(default_factory=dict)
  ```

  **建议**

  ```
      tags: Mapping[str, Any] = field(default_factory=dict, compare=False)
  ```

  </details>

## `src/core/vault.py`

待办 2 / 共 4

- [x] **#64** · `high/bug` · `L77-L79` · `None` search text silently wipes the search index: - `_set_search` unconditionally deletes the existing searc… ✅
  <details><summary>详情与建议</summary>

  `None` search text silently wipes the search index:
- `_set_search` unconditionally deletes the existing search row and only re-inserts when `search_text` is truthy. Since `search_text` defaults to `None` (and `Block.save()` also defaults to `None`), every ordinary re-save re-runs this path, so an object that was previously indexed silently loses its searchability.
- `rebuild_index()` with the default `text_of=None` first deletes every row from `search` and then never inserts anything, so a plain `rebuild_index()` empties the index entirely while still returning a non-zero, misleading count (blocks processed, not entries indexed).
Treat `None` as "leave the index untouched" / validate `text_of` before wiping (or provide a default extractor), and return the number of rows actually written.

  **现有代码**

  ```
  77        created = not self.bucket.has(block.id)
  78        self.bucket.put(block)
  79        self._set_search(block.id, search_text)
  ```

  **建议**

  ```
          created = not self.bucket.has(block.id)
          self.bucket.put(block)
          if search_text is not None:
              self._set_search(block.id, search_text)
  ```

  </details>
- [x] **#65** · `medium/bug` · `L146` · The query is embedded raw into a `LIKE` pattern, so user-supplied `%` and `_` are interpreted as wildcards (`%… ✅
  <details><summary>详情与建议</summary>

  The query is embedded raw into a `LIKE` pattern, so user-supplied `%` and `_` are interpreted as wildcards (`%` alone matches everything) instead of literal characters. Escape them and declare an `ESCAPE` clause so the search behaves as a substring match.

  **现有代码**

  ```
  146rows = self.bucket.query("SELECT oid FROM search WHERE body LIKE ?", (f"%{query}%",))
  ```

  **建议**

  ```
          escaped = query.replace("\\", "\\\\").replace("%", "\\%").replace("_", "\\_")
          rows = self.bucket.query(
              "SELECT oid FROM search WHERE body LIKE ? ESCAPE '\\'", (f"%{escaped}%",)
          )
          return [Oid.parse(str(row["oid"])) for row in rows]
  ```

  </details>
- [ ] **#66** · `medium/maintainability` · `L168-L170` · `Vault` silently discards optional parameters that callers rely on: - `gc(retention_ms=...)` ignores `retentio…
  <details><summary>详情与建议</summary>

  `Vault` silently discards optional parameters that callers rely on:
- `gc(retention_ms=...)` ignores `retention_ms` and always returns `0`, so a caller requesting retention-based collection gets a no-op with no error or warning that the option was ignored;
- `verify(deep=True)` drops `deep`, so a caller asking for deep/content-level verification receives the cheap scan's result with no indication the flag was not honored.
Either implement the semantics or fail loudly (e.g. `raise NotImplementedError`) so the caller is not misled.

  **现有代码**

  ```
  168    def gc(self, *, retention_ms: int | None = None) -> int:
  169        del retention_ms
  170        return 0
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#67** · `medium/bug` · `L213-L216` · If `attrs["tags"]` is a plain string (a plausible caller mistake, `str` is also `Iterable[str]`), iterating it…
  <details><summary>详情与建议</summary>

  If `attrs["tags"]` is a plain string (a plausible caller mistake, `str` is also `Iterable[str]`), iterating it yields one tag per character, silently producing corrupt tag data instead of a single tag. Handle `str` explicitly before the generic iterable fallback. The same caveat applies to `_wanted_tags`, which accepts `Iterable[str] | Mapping` and will split a string argument into characters.

  **现有代码**

  ```
  213    raw = attrs.get("tags") or {}
  214    if isinstance(raw, Mapping):
  215        return {str(key): value for key, value in raw.items()}
  216    return {str(tag): None for tag in raw}
  ```

  **建议**

  ```
      raw = attrs.get("tags") or {}
      if isinstance(raw, Mapping):
          return {str(key): value for key, value in raw.items()}
      if isinstance(raw, str):
          return {raw: None} if raw else {}
      return {str(tag): None for tag in raw}
  ```

  </details>

# 模块 `feature`

## `src/feature/note/__init__.py`

待办 1 / 共 1

- [ ] **#56** · `low/maintainability` · `L26-L28` · `LineKind` is a redundant public alias: it is imported from the same `.data` module as `Line`, and `data.Line`…
  <details><summary>详情与建议</summary>

  `LineKind` is a redundant public alias: it is imported from the same `.data` module as `Line`, and `data.Line` is simply a re-export of `shared.canvas.Line` (an `IntEnum`). So `note.Line`, `note.LineKind` are the exact same object (`Line is LineKind` → True), while the note line-dict type is exposed as `LineDict`. Exporting two names for one object adds no value and invites confusion: a caller reading `from feature.note import Line` in a note context may expect the line dict (`LineDict`) rather than the canvas enum. Consider dropping the duplicate alias (and its `__all__` entry), or, if the descriptive name is wanted, keep only `LineKind` for the enum and stop exporting `Line` as a second spelling.

  **现有代码**

  ```
  26from .data import (
  27    Line as LineKind,
  28)
  ```

  **建议**

  _（无）_

  </details>

## `src/feature/note/data.py`

待办 1 / 共 2

- [x] **#91** · `medium/bug` · `L138-L139` · Dedup key口径与落盘负载口径不一致：`body_hash()`（内容哈希，剥离行 id）同时被 `Bucket.put` 当作 `body_id`/checksum（`checksum = block.body_… ✅
  <details><summary>详情与建议</summary>

  Dedup key口径与落盘负载口径不一致：`body_hash()`（内容哈希，剥离行 id）同时被 `Bucket.put` 当作 `body_id`/checksum（`checksum = block.body_hash()`；内容池按它去重），但真正落盘的负载 `to_data()` 里 `text` 仍带行 id。于是「正文 + 行内样式相同、行 id 不同」的两条笔记（例如两条空笔记、或复制出来的笔记）会命中同一内容行：第二个块不再存自己的负载，读回时拿到的是第一个块的行 id（行 id 被静默替换）。行 id 是 `style`、`_state()`/版本历史、编辑器操作的唯一身份，因此会带来无意义的历史 diff、`restore` 结果与落盘内容来回震荡等问题。建议让去重键与负载口径一致：把行 id 移出被哈希的 body 负载（作为不参与哈希的伴生数据存储），或直接由负载字节派生去重键，保证同一个 hash 不会对应两份不同的负载。

  **现有代码**

  ```
  138    def body_hash(self) -> str:
  139        return self.body.refresh().hash
  ```

  **建议**

  ```
      # ---- 去重键（剥离行 id；只算正文 + 行内样式）----
      # 注意：该键同时被 Bucket 用作 body_id 去重，故负载 to_data() 必须与 content() 口径一致
      def body_hash(self) -> str:
          return self.body.refresh().hash
  ```

  </details>
- [ ] **#92** · `low/bug` · `L165` · The read view does an unguarded `Oid.parse` on each stored value: `access` is a freely assignable string list …
  <details><summary>详情与建议</summary>

  The read view does an unguarded `Oid.parse` on each stored value: `access` is a freely assignable string list and `NoteService.add_access(..., oid: Oid | str)` only does `str(oid)` without validation, so a single invalid/dirty id makes the read-only accessor raise `InvalidIdError` and turns the view into an exception source. Filter items that fail to parse (or validate on the write path when adding an access entry) instead of letting the exception escape. (`Oid` has no `try_parse`; use `try`/`except` around `Oid.parse` or validate before storing.)

  **现有代码**

  ```
  165        return tuple(Oid.parse(str(oid)) for oid in self.access if oid)
  ```

  **建议**

  ```
          return tuple(ref for oid in self.access if oid and (ref := Oid.try_parse(str(oid))))
  ```

  </details>

## `src/feature/note/edit/body.py`

待办 1 / 共 1

- [ ] **#71** · `medium/bug` · `L49-L50` · `normalize_body` iterates `raw` directly, but the parameter is typed `Any` and the docstring promises "字符串按 `\…
  <details><summary>详情与建议</summary>

  `normalize_body` iterates `raw` directly, but the parameter is typed `Any` and the docstring promises "字符串按 `\n` 拆行". When `raw` is itself a plain string (e.g. a legacy bare text body reaching `NoteBody.from_data` -> `cls(text=data)`), `for item in raw` walks the string character by character: `"a\nb"` becomes `["a", "\n" -> ["", ""], "b"]`, i.e. one line per character instead of two lines. The same level-confusion hits a single line `Mapping` passed as `raw` (its keys get iterated). Normalize a bare string (and a single mapping) before the loop.

  **现有代码**

  ```
  49    for item in raw or ():
  50        if is_marker(item):
  ```

  **建议**

  ```
      if isinstance(raw, str):
          raw = raw.split("\n")
      for item in raw or ():
          if is_marker(item):
  ```

  </details>

## `src/feature/note/edit/style.py`

待办 2 / 共 2

- [ ] **#83** · `medium/bug` · `L252-L254` · `set_range_style` copies patch values straight into the `Style` record without any validation: unknown field n…
  <details><summary>详情与建议</summary>

  `set_range_style` copies patch values straight into the `Style` record without any validation: unknown field names are silently ignored (`if key in data`), and wrong-typed values are persisted verbatim, e.g. `{"size": "12"}` or `{"bold": 1}` yields a `Style` whose `size`/`bold` violate the declared field types, and a `None` value wipes a field to `None`. This corrupts the on-disk style data and only surfaces later in rendering/serialization. Validate/coerce the allowed fields (or reject the patch) before constructing the style.

  **现有代码**

  ```
  252    for key, value in patch.items():
  253        if key in data:
  254            data[key] = value
  ```

  **建议**

  ```
      for key, value in patch.items():
          if value is None or key not in data:
              continue
          if key in _BOOL_KEYS:
              data[key] = bool(value)
          elif key == "size":
              data[key] = float(value)
          elif key in ("font", "color"):
              data[key] = str(value)
  ```

  </details>
- [ ] **#82** · `medium/bug` · `L34` · Style decoding assumes the persisted value is a Mapping without guarding: - `Style.from_data` iterates `key in…
  <details><summary>详情与建议</summary>

  Style decoding assumes the persisted value is a Mapping without guarding:
- `Style.from_data` iterates `key in data`; a `null`/scalar entry (e.g. `{"style": {"lid": [[0, 5, null]]}}`) raises `TypeError: argument of type 'NoneType' is not iterable`, propagating out of `coerce_style` → `NoteBody.__init__` and breaking note loading.
- `decode_style` is reached through `coerce_style(value: Any, ...)` whose contract is "任意样式输入", but the only shape check is `isinstance(raw, (list, tuple))`; any other truthy non-mapping value (e.g. `"style": 5` or `"bold"`) reaches `raw.items()` and raises `AttributeError`.
Add `isinstance(..., Mapping)` guards (and fall back to the default style / empty map) so malformed entries degrade gracefully instead of crashing note loading.

  **现有代码**

  ```
  34        known = {key: data[key] for key in cls.__dataclass_fields__ if key in data}
  ```

  **建议**

  ```
          if not isinstance(data, Mapping):
              return cls()
          known = {key: data[key] for key in cls.__dataclass_fields__ if key in data}
  ```

  </details>

## `src/feature/note/edit/text.py`

待办 3 / 共 3

- [ ] **#68** · `medium/bug` · `L61-L62` · When `after_id` is not `None` but does not exist in `lines`, `index_of` returns `-1`, so `position` becomes `0…
  <details><summary>详情与建议</summary>

  When `after_id` is not `None` but does not exist in `lines`, `index_of` returns `-1`, so `position` becomes `0` and the new line is silently inserted at the *front* instead of after the target (or being rejected). The `max(0, position)` guard can never trigger anyway, since `index_of(...) + 1 >= 0`. This makes a stale/unknown `line_id` (e.g. from `ctx.line_id` in tools.py) corrupt line order silently. Consider explicitly handling the not-found case — append at the end, or signal the error to the caller.

  **现有代码**

  ```
  61    position = len(out) if after_id is None else index_of(out, after_id) + 1
  62    out.insert(max(0, position), entry(lid, str(value)))
  ```

  **建议**

  ```
      if after_id is None:
          position = len(out)
      else:
          found = index_of(out, after_id)
          position = len(out) if found < 0 else found + 1
      out.insert(position, entry(lid, str(value)))
  ```

  </details>
- [ ] **#69** · `low/performance` · `L102-L103` · This no-op branch discards the `out` copy already produced at the top of the function and allocates a second f…
  <details><summary>详情与建议</summary>

  This no-op branch discards the `out` copy already produced at the top of the function and allocates a second full copy via `copy_lines(lines)`. It is functionally equivalent to returning `out`, so the extra allocation is wasted (this path can run per keystroke via `service.merge_line`). Return `out` instead.

  **现有代码**

  ```
  102    if is_marker(previous["v"]) or is_marker(current["v"]):
  103        return copy_lines(lines), None, 0
  ```

  **建议**

  ```
      if is_marker(previous["v"]) or is_marker(current["v"]):
          return out, None, 0
  ```

  </details>
- [ ] **#70** · `low/maintainability` · `L68` · Inconsistent copy semantics for line payloads.
  <details><summary>详情与建议</summary>

  Inconsistent copy semantics for line payloads. `apply_text` defensively deep-copies marker payloads (`dict(line["v"])`), while `remove_line` / `set_line_text` / `split_line` pass `line["v"]` by reference into the new `entry(...)`, so a marker dict ends up aliased between the input and the returned sequence. If any later code mutates a marker payload in place, the change unexpectedly propagates to the other sequence. Use the same `dict(...)` copy for marker values (or document the shared-reference contract) across all four helpers.

  **现有代码**

  ```
  68    out = [entry(line["id"], line["v"], line.get("p")) for line in lines if line["id"] != line_id]
  ```

  **建议**

  _（无）_

  </details>

## `src/feature/note/service.py`

待办 2 / 共 3

- [ ] **#88** · `medium/bug` · `L166-L167` · `order` 未做任何校验就在这里直接当旧下标使用： - 越界下标 → `IndexError`；
  <details><summary>详情与建议</summary>

  `order` 未做任何校验就在这里直接当旧下标使用：
- 越界下标 → `IndexError`；
- 重复下标 → 同一行对象被插入两次，正文出现**重复行 id**，而 set_line / remove_line / drop_style / _state() / 版本补丁（`{line["id"]: ...}`）全都以行 id 为键，会造成改一行、样式跟错、版本 diff 丢行等连锁错误；
- `order` 短于行数 → 悄悄丢行。
建议先校验它确实是 0..len-1 的排列再重排。

  **现有代码**

  ```
  166        lines = list(data.body.text)
  167        data.body.text = [lines[index] for index in order]
  ```

  **建议**

  ```
          lines = list(data.body.text)
          if sorted(order) != list(range(len(lines))):
              raise ValueError("order 必须是 0..len(lines)-1 的排列")
          data.body.text = [lines[index] for index in order]
  ```

  </details>
- [x] **#89** · `medium/bug` · `L301-L304` · Version replay starts from the in-memory `data._state()`, which may be dirty, while the version chain is ancho… ✅
  <details><summary>详情与建议</summary>

  Version replay starts from the in-memory `data._state()`, which may be dirty, while the version chain is anchored to the last persisted state (`data._saved_state`). This affects both `state_at` and `restore`: the other edit methods (set_text / set_line / toggle_style…) intentionally only change memory, so with unpersisted edits the reverse patches are applied to the wrong baseline, returning incorrect historical content/styles and, for `restore`, writing the wrong history back and then persisting it as a new version via `save()`. Prefer the persisted baseline.

  **现有代码**

  ```
  301            data._state(),  # noqa: SLF001 — 域服务读取数据内部状态
  302            str(version),
  303        )
  304        body: list[LineDict] = state["body"]
  ```

  **建议**

  ```
              data._saved_state or data._state(),  # noqa: SLF001 — 回放起点必须是落盘基线
              str(version),
          )
          body: list[LineDict] = state["body"]
  ```

  </details>
- [ ] **#90** · `low/bug` · `L191` · 当 `line_id` 不存在、或它指向嵌入占位（`is_marker`）时，底层 `split_line` 会原样返回行序列（占位行不拆分、未知 id 无匹配），但这里仍把新生成的 `new_lid` 返回给调用方—
  <details><summary>详情与建议</summary>

  当 `line_id` 不存在、或它指向嵌入占位（`is_marker`）时，底层 `split_line` 会原样返回行序列（占位行不拆分、未知 id 无匹配），但这里仍把新生成的 `new_lid` 返回给调用方——这是一个**正文里并不存在的行 id**。编辑器据此定位光标/后续编辑会指向空行或落到错误的行上。建议在无可拆目标时回退返回原 `line_id`（或显式报错）。

  **现有代码**

  ```
  191        data.body.text, new_lid, _, _ = split_line(data.body.text, line_id, offset)
  ```

  **建议**

  ```
          if not any(
              line["id"] == line_id and not is_marker(line["v"]) for line in data.body.text
          ):
              return line_id
          data.body.text, new_lid, _, _ = split_line(data.body.text, line_id, offset)
  ```

  </details>

## `src/feature/note/shapes.py`

待办 5 / 共 5

- [ ] **#72** · `medium/bug` · `L51-L52` · `_param()` keeps `default=None` when the JSON omits `"default"`, yet `defaults()` still inserts that key with …
  <details><summary>详情与建议</summary>

  `_param()` keeps `default=None` when the JSON omits `"default"`, yet `defaults()` still inserts that key with a `None` value. The generators rely on `params.get(key, fallback)`, and because the key *is* present, the documented fallback is never used — `float(params.get("top", 0.5))` / `int(params.get("sides", 6))` then raise `TypeError: float() argument must be ... not 'NoneType'`. Either drop keys whose default is `None`, or make the generators tolerate `None`.

  **现有代码**

  ```
  51    def defaults(self) -> dict[str, Any]:
  52        return {param.key: param.default for param in self.params}
  ```

  **建议**

  ```
      def defaults(self) -> dict[str, Any]:
          # 缺省未声明时不要塞 None，否则生成器的 params.get(key, fallback) 拿不到 fallback
          return {
              param.key: param.default
              for param in self.params
              if param.default is not None
          }
  ```

  </details>
- [ ] **#73** · `medium/maintainability` · `L96-L97` · `for_ui()` iterates only `self.shapes` while `get()`/`by_key()` search `(*self.shapes, *self.lines)`, so the `…
  <details><summary>详情与建议</summary>

  `for_ui()` iterates only `self.shapes` while `get()`/`by_key()` search `(*self.shapes, *self.lines)`, so the `lines` section of `config/shapes.json` (straight/curve/dashed, with their params) is invisible to the UI and cannot be round-tripped through the same metadata path. If the exclusion is intentional, document it; otherwise include `self.lines` here as well.

  **现有代码**

  ```
  96            for spec in self.shapes
  97        ]
  ```

  **建议**

  ```
              for spec in (*self.shapes, *self.lines)
          ]
  ```

  </details>
- [ ] **#75** · `low/bug` · `L130-L131` · `_spec()` indexes `raw["id"]`/`raw["key"]` directly, so a hand-edited or malformed `config/shapes.json` (or a …
  <details><summary>详情与建议</summary>

  `_spec()` indexes `raw["id"]`/`raw["key"]` directly, so a hand-edited or malformed `config/shapes.json` (or a file pointed at by `CAIRN_SHAPES`) surfaces as a bare `KeyError` with no context about which entry is broken. Add explicit validation with a readable message; the same place is the natural spot to reject duplicate ids, which are currently silently shadowed by the first match in `get()`/`by_key()`.

  **现有代码**

  ```
  130        id=int(raw["id"]),
  131        key=str(raw["key"]),
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#74** · `low/bug` · `L268` · `build_vertices()` already merged `spec.defaults()`, so passing the merged `values` down again is redundant an…
  <details><summary>详情与建议</summary>

  `build_vertices()` already merged `spec.defaults()`, so passing the merged `values` down again is redundant and, worse, duplicates the merge logic that differs subtly (here `None`/falsy values are silently coerced to `0.0`). If `Param.type` is anything non-numeric (the field is an unrestricted string and is exported to the UI), `float(...)` raises a bare `ValueError` with no indication of which param is at fault; `or 0.0` also turns `False`/`None` into `0.0` without reporting. Validate the param type/format at load time (or raise a descriptive error here).

  **现有代码**

  ```
  268    ordered = [float(values.get(param.key, 0.0) or 0.0) for param in spec.params]
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#76** · `low/performance` · `L64-L65` · `ShapeSet.get()`/`by_key()` rebuild `(*self.shapes, *self.lines)` on every call (linear scan).
  <details><summary>详情与建议</summary>

  `ShapeSet.get()`/`by_key()` rebuild `(*self.shapes, *self.lines)` on every call (linear scan). If these are called per shape/line on a hot rendering path, build an id/key index once on the `ShapeSet` instead of repeating the tuple concatenation and scan.

  **现有代码**

  ```
  64    def get(self, form_id: int) -> ShapeSpec | None:
  65        for spec in (*self.shapes, *self.lines):
  ```

  **建议**

  _（无）_

  </details>

## `src/feature/note/tools.py`

待办 5 / 共 5

- [ ] **#77** · `medium/bug` · `L258-L259` · `SetParagraphTool` treats "missing key" and "explicit value" inconsistently for the paragraph *default*.
  <details><summary>详情与建议</summary>

  `SetParagraphTool` treats "missing key" and "explicit value" inconsistently for the paragraph *default*. `{"align": "left"}` is registered with `toggle=True`, but `state()` returns `False` whenever the `align` key is absent, even though a line without `align` renders left-aligned. Consequences: (1) the left-align button never highlights on default lines; (2) clicking it on a line that already has `align="left"` takes the toggle-clear branch and removes the key, so the highlight disappears while the text stays left-aligned — the click looks like a no-op followed by a state change. Please give the tool an explicit "default value" (compare with `ctx.paragraph.get(key, self._default)` and use the same default in `run`'s toggle check) or drop `toggle` for `align-left`/`heading=None` so defaults are treated as "not set but equivalent".

  **现有代码**

  ```
  258            elif ctx.paragraph.get(key) != value:
  259                return False
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#78** · `low/maintainability` · `L125` · `SetStyleTool` is exported and named in the module docstring as one of the core subclasses, but it is never in…
  <details><summary>详情与建议</summary>

  `SetStyleTool` is exported and named in the module docstring as one of the core subclasses, but it is never instantiated or referenced anywhere in the codebase (only `ToggleStyleTool` / cycle tools are registered). Its `state()` also samples a single point (`start, _ = ctx.span()`) and can only return `True`/`False`, so on a multi-character selection it claims "生效" when only the first character carries the style, and it never returns the tri-state `None` promised by the module contract. Either register the concrete variants it was meant for (e.g. explicit colour/font picks) or remove it; if kept, aggregate the state across the whole selection like `ToggleStyleTool` does via `bool_state`.

  **现有代码**

  ```
  125class SetStyleTool(Tool):
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#79** · `low/maintainability` · `L173-L174` · `ColorCycleTool` / `FontCycleTool` are edit-type tools but inherit the base `Tool.state`, which always returns…
  <details><summary>详情与建议</summary>

  `ColorCycleTool` / `FontCycleTool` are edit-type tools but inherit the base `Tool.state`, which always returns `None`. The module docstring states that "编辑型" tools expose a readable state for toolbar highlighting, yet the current colour/font cannot be reflected at all (the UI cannot even show which palette/font entry is active). Add a `state()` override (e.g. report whether `style_at(...)` value is in `self._palette` / `self._fonts`) or update the docstring to call out that cycle tools are highlight-less by design.

  **现有代码**

  ```
  173    id = "font"
  174    text = "Aa"
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#80** · `low/bug` · `L302` · `ctx.line_id or None` silently maps an empty line id to `None`, which `Note.insert_line_after` interprets as "…
  <details><summary>详情与建议</summary>

  `ctx.line_id or None` silently maps an empty line id to `None`, which `Note.insert_line_after` interprets as "append at the end of the note". If the caller passes `""` because the focus/selection context was lost, the code block ends up at the bottom of the document instead of relative to the intended line — a silent wrong placement rather than an error. Please confirm this fallback is intentional and document it at the call site (or require a non-empty line id).

  **现有代码**

  ```
  302        new_id = note.insert_line_after(data, ctx.line_id or None, "")
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#81** · `low/maintainability` · `L467-L468` · `run_tool` returns `True` unconditionally once the tool is registered and available, so it does not reflect wh…
  <details><summary>详情与建议</summary>

  `run_tool` returns `True` unconditionally once the tool is registered and available, so it does not reflect whether the mutation actually applied (e.g. `set_paragraph` with a stale `line_id` silently does nothing), and any exception raised inside `tool.run` propagates to the caller with no indication that a previous call may already have partially mutated `data`. Either document this contract explicitly in the docstring ("True = dispatched, not = applied; exceptions propagate") or have the note-mutating methods signal failure so the UI can react.

  **现有代码**

  ```
  467    tool.run(note, data, ctx)
  468    return True
  ```

  **建议**

  _（无）_

  </details>

## `src/feature/note/versions.py`

待办 0 / 共 1

- [x] **#87** · `critical/bug` · `L123-L128` · Line ordering after a patch is not explicit: `diff` serializes with `core.storage.canonical` (= `cbor2.dumps(c… ✅
  <details><summary>详情与建议</summary>

  Line ordering after a patch is not explicit: `diff` serializes with `core.storage.canonical` (= `cbor2.dumps(canonical=True)`, which sorts map keys) and `VersionStore.patch()` decodes it back with `cbor2.loads`, so `patch.items()` in `apply` yields keys sorted by (len, bytes) — ULID order, not layout order. Re-inserted (PUT) lines are placed after `entry["after"]` (falling back to position 0 when the anchor has not been inserted yet), so replaying can reorder lines with no warning. Concrete failure: old `[S("01A"), D1("01Z"), D2("01M")]`, new `[S]`; `diff` produces `{D1: after S, D2: after D1}`, canonical sorts it to `D2, D1`; `apply` processes D2 first (anchor missing → insert at 0) → `[D2, S]`, then D1 → `[D2, S, D1]` instead of `[S, D1, D2]`. Neither `@order` (present only when the common subsequence changes) nor any error repairs the order. Make the final order explicit: always emit `@order` whenever lines are added/removed (so `apply`'s final `body.sort(...)` is authoritative), and/or order the insertions by resolving the `after` chains before mutating `body`.

  **现有代码**

  ```
  123            after = entry.get("after")
  124            position = 0
  125            if after:
  126                index = next((i for i, line in enumerate(body) if line["id"] == after), -1)
  127                position = index + 1 if index >= 0 else 0
  128            body.insert(position, record)
  ```

  **建议**

  _（无）_

  </details>

## `src/feature/project/__init__.py`

待办 2 / 共 3

- [x] **#84** · `medium/bug` · `L123-L124` · `add_member` 无幂等保护：重复把同一成员加入项目会在 `relation` 表里生成多条 `src--contains-->dst` 平行的边，`members()`（第 129 行） accordingly… ✅
  <details><summary>详情与建议</summary>

  `add_member` 无幂等保护：重复把同一成员加入项目会在 `relation` 表里生成多条 `src--contains-->dst` 平行的边，`members()`（第 129 行） accordingly 会返回重复的 Oid（下游按“成员集合”使用时会重复渲染/重复计数）。对照 `shared/group.py` 的 `GroupData.add` 是先做 `ref not in self.group` 去重再建边。建议这里先查一次现有出边并在命中时直接返回既有边（避免为查成员而引入运行期的 `Oid` 导入，用 `str()` 比较即可）。

  **现有代码**

  ```
  123        """把一个对象加为项目成员（建 ``contains`` 边）。"""
  124        return Relation.create(self.vault, data.oid, member, relation=relation, domain="project")
  ```

  **建议**

  ```
          """把一个对象加为项目成员（已存在则直接返回既有边，保证幂等）。"""
          target = str(member)
          for edge in Relation.outbound(self.vault, data.oid, relation=relation):
              if str(edge.target) == target:
                  return edge
          return Relation.create(self.vault, data.oid, member, relation=relation, domain="project")
  ```

  </details>
- [ ] **#86** · `low/maintainability` · `L105-L106` · `update` merges `props` after `description`, while `create` does the opposite (lets an explicit `description` …
  <details><summary>详情与建议</summary>

  `update` merges `props` after `description`, while `create` does the opposite (lets an explicit `description` override `props`), so the same field is prioritized inconsistently across the two entry points and callers can get unexpected results; `if props:` also makes an empty dict a no-op, so props cannot be cleared the way `description=None` can. Unify the merge order across both entry points (merge `props` first, then let an explicit `description` win, matching `create`) and define the empty-dict semantics explicitly (clear vs ignore).

  **现有代码**

  ```
  105        if props:
  106            merged.update(props)
  ```

  **建议**

  ```
          if props is not None:
              merged.update(props)
          if description is not UNSET:
              if description is None:
                  merged.pop("description", None)
              else:
                  merged["description"] = str(description)
  ```

  </details>
- [ ] **#85** · `low/maintainability` · `L28` · 这里重复定义了关系词表常量 `CONTAINS`，而 `shared/relation.py` 已导出同义常量并且 `shared/group.py` 是 `from .relation import CONTAINS …
  <details><summary>详情与建议</summary>

  这里重复定义了关系词表常量 `CONTAINS`，而 `shared/relation.py` 已导出同义常量并且 `shared/group.py` 是 `from .relation import CONTAINS as _CONTAINS` 引用的。两处各自维护字符串容易漂移（例如将来把边名改成 `member-of` 时只改一处，项目成员查询就会静默失配）。建议直接复用共享常量。

  **现有代码**

  ```
  28CONTAINS = "contains"
  ```

  **建议**

  ```
  # 常量统一由 shared.relation 提供：from ..shared.relation import CONTAINS, Relation
  ```

  </details>

## `src/feature/shared/asset.py`

待办 3 / 共 3

- [ ] **#116** · `medium/bug` · `L75` · `mime` is declared as a `ClassVar` defaulting to `None`, but the real per-instance media type is written into …
  <details><summary>详情与建议</summary>

  `mime` is declared as a `ClassVar` defaulting to `None`, but the real per-instance media type is written into `attrs["mime"]` in `create()` and read back only through the `content_type` property. Two consequences:
1. `asset.mime` (the natural name, and the convention used by `NoteData.mime` / `GroupData.mime`) always returns `None`, even though a MIME was stored — a silent wrong value for any caller using it.
2. Because it is a `ClassVar` and not an `Attr`, `mime` is not collected by `collect_fields()`, so it never appears in the type's field metadata, unlike every other real field of this block.
Suggest making it a declared field (single source of truth) and writing it through the descriptor in `create()`.

  **现有代码**

  ```
  75    mime: ClassVar[str | None] = None
  ```

  **建议**

  ```
      mime: Attr[str | None] = None  # 转码后的统一编码（落 attrs['mime']）
  ```

  </details>
- [ ] **#117** · `low/maintainability` · `L107-L108` · These fields are declared with `Attr` descriptors but are written directly into `attrs[...]`, bypassing `Attr.…
  <details><summary>详情与建议</summary>

  These fields are declared with `Attr` descriptors but are written directly into `attrs[...]`, bypassing `Attr.__set__` (note `title` right below *is* set through the descriptor). Today it happens to be behaviour-equivalent because none of them has a `coerce`/`item`, but the two write paths can silently diverge as soon as one is added, and it makes the declared-field contract inconsistent. Prefer assigning through the descriptor (same for `data.attrs["name"]` above).

  **现有代码**

  ```
  107        data.attrs["mime"] = unified
  108        data.attrs["origin_mime"] = original
  ```

  **建议**

  ```
          data.mime = unified
          data.origin_mime = original
  ```

  </details>
- [ ] **#118** · `low/bug` · `L67` · For a stream source, `read()` is returned unchecked.
  <details><summary>详情与建议</summary>

  For a stream source, `read()` is returned unchecked. `BinaryIO` is only a type hint, so a text-mode file object (or any non-binary stream) yields `str`, which is then stored as the block body and only blows up much later (e.g. in `canonical()` / `bytes()`), far from the real cause. Validating the payload here gives a clear error at the ingestion point. (Not closing a caller-owned stream is intentional, so that part is fine.)

  **现有代码**

  ```
  67    return source.read()
  ```

  **建议**

  ```
      payload = source.read()
      if not isinstance(payload, (bytes, bytearray, memoryview)):
          raise TypeError("资产源必须返回 bytes（不要用文本模式打开）")
      return bytes(payload)
  ```

  </details>

## `src/feature/shared/base.py`

待办 1 / 共 3

- [x] **#94** · `medium/bug` · `L24` · `str` itself satisfies `Iterable[str]`, so a lone tag string silently degenerates into one entry per character… ✅
  <details><summary>详情与建议</summary>

  `str` itself satisfies `Iterable[str]`, so a lone tag string silently degenerates into one entry per character: `normalize_tags("abc") == {"a": None, "b": None, "c": None}`. Callers such as `NoteService.create(..., tags=...)` (typed `Iterable[str] | Mapping[str, Any] | None`) are annotation-legal when given a bare string, so this produces wrong tags without any error. Guard against a lone string explicitly.

  **现有代码**

  ```
  24    return {str(tag): None for tag in value}
  ```

  **建议**

  ```
      if isinstance(value, str):
          raise TypeError("normalize_tags expects a tag collection, got a single string")
      return {str(tag): None for tag in value}
  ```

  </details>
- [ ] **#95** · `low/bug` · `L23` · Coercing both keys and values with `str()` is lossy: distinct keys such as `1` and `"1"` collapse into a singl…
  <details><summary>详情与建议</summary>

  Coercing both keys and values with `str()` is lossy: distinct keys such as `1` and `"1"` collapse into a single entry (the later one silently wins), so the mapping may come back with fewer tags than passed in. Consider preserving the original key when it is already a `str` and only stringifying non-string keys, or rejecting non-string keys instead of silently merging them.

  **现有代码**

  ```
  23        return {str(key): (None if item is None else str(item)) for key, item in value.items()}
  ```

  **建议**

  _（无）_

  </details>
- [x] **#96** · `low/maintainability` · `L40-L41` · `normalize_tags` is a public, documented helper used across the feature layer but is missing from `__all__`, w… ✅
  <details><summary>详情与建议</summary>

  `normalize_tags` is a public, documented helper used across the feature layer but is missing from `__all__`, while the other public names are listed. It is therefore excluded from `from ...base import *` and the module's declared public surface is inconsistent.

  **现有代码**

  ```
  40__all__ = [
  41    "UNSET",
  ```

  **建议**

  ```
  __all__ = [
      "UNSET",
      "normalize_tags",
  ```

  </details>

## `src/feature/shared/canvas.py`

待办 1 / 共 3

- [x] **#103** · `high/bug` · `L133-L135` · `point_count` / `param_count` come straight from persisted data and are never range-checked. ✅
  <details><summary>详情与建议</summary>

  `point_count` / `param_count` come straight from persisted data and are never range-checked. A negative count makes `values[8 + point_count]` wrap around (silently reading the wrong element) and an oversized count raises `IndexError`. `Block.decode()` only catches `CBORDecodeError/EncodeError/KeyError/TypeError/ValueError` (see core/storage/block.py), so this `IndexError` escapes un-wrapped instead of becoming a `CorruptObjectError`, and truncated/corrupt canvases crash loading. Validate both counts before indexing/slicing.

  **现有代码**

  ```
  133        point_count = int(values[7])
  134        points = values[8 : 8 + point_count]
  135        param_count = int(values[8 + point_count])
  ```

  **建议**

  ```
          point_count = int(values[7])
          if point_count < 0 or 8 + point_count >= len(values):
              raise ValueError("图形点数非法")
          points = values[8 : 8 + point_count]
          param_count = int(values[8 + point_count])
          if param_count < 0 or 9 + point_count + param_count > len(values):
              raise ValueError("图形参数个数非法")
  ```

  </details>
- [x] **#104** · `medium/bug` · `L154-L155` · `Link.from_seq` indexes `values[0..2]` without any length check, unlike `Graphic.from_seq` which raises `Value… ✅
  <details><summary>详情与建议</summary>

  `Link.from_seq` indexes `values[0..2]` without any length check, unlike `Graphic.from_seq` which raises `ValueError`; a short/corrupt link sequence raises an `IndexError` that is not in the exception tuple caught by `Block.decode()`, so it propagates as a raw error instead of a `CorruptObjectError`. `Paint.from_seq` has the same missing validation but silently returns a default `Paint` when the sequence is shorter than 8 values, masking truncated/corrupt data and dropping stroke/fill/alpha information. Both should raise `ValueError` on insufficient input so `Block.decode()` can turn it into a `CorruptObjectError`.

  **现有代码**

  ```
  154        values = [float(value) for value in seq]
  155        return cls(int(values[0]), int(values[1]), int(values[2]))
  ```

  **建议**

  ```
          values = [float(value) for value in seq]
          if len(values) < 3:
              raise ValueError("连线序列过短")
          return cls(int(values[0]), int(values[1]), int(values[2]))
  ```

  </details>
- [ ] **#105** · `low/maintainability` · `L168` · `mode` is accepted verbatim (and `CanvasBody.from_data` restores it verbatim as well), so the declared `CANVAS…
  <details><summary>详情与建议</summary>

  `mode` is accepted verbatim (and `CanvasBody.from_data` restores it verbatim as well), so the declared `CANVAS_MODE` whitelist is never enforced and never referenced anywhere. A typo/invalid mode (e.g. "dragram") is persisted and replicated into the content hash, breaking the renderer's assumption that only diagram/sketch exist. Validate against `CANVAS_MODE` at the single entry point.

  **现有代码**

  ```
  168        self.mode = str(mode)
  ```

  **建议**

  ```
          self.mode = str(mode)
          if self.mode not in CANVAS_MODE:
              raise ValueError(f"未知画板模式：{self.mode}")
  ```

  </details>

## `src/feature/shared/group.py`

待办 2 / 共 3

- [x] **#106** · `medium/bug` · `L120-L121` · `move()` indexes `items` with the caller-supplied `order` without checking that it is a permutation of `0..n-1… ✅
  <details><summary>详情与建议</summary>

  `move()` indexes `items` with the caller-supplied `order` without checking that it is a permutation of `0..n-1`:
- an out-of-range index raises a bare `IndexError` (not `GroupError`) *after* nothing was saved;
- a duplicated index silently duplicates a child in `group`;
- a missing index silently drops a child while its `contains` relation row survives, so the structure and the relation table silently drift.
Validate the permutation and raise `GroupError` before mutating.

  **现有代码**

  ```
  120        items = list(self.group)
  121        self.group = [items[index] for index in order]
  ```

  **建议**

  ```
          items = list(self.group)
          if sorted(order) != list(range(len(items))):
              raise GroupError(f"重排下标必须是 0..{len(items) - 1} 的完整排列: {list(order)}")
          self.group = [items[index] for index in order]
  ```

  </details>
- [ ] **#108** · `low/maintainability` · `L113-L114` · `remove()` only strips the list entry and leaves the `contains` row in place, and `Relation` currently exposes…
  <details><summary>详情与建议</summary>

  `remove()` only strips the list entry and leaves the `contains` row in place, and `Relation` currently exposes no delete/cleanup API in this codebase, so the promised "后续清理策略" does not exist and the relation table can only grow stale edges. A concrete symptom: `remove(child)` followed by `add(child)` inserts a *second* `contains` row for the same pair (no unique constraint on `src`/`dst`/`kind`), and `Project`-style `outbound(...)` consumers will keep seeing the removed member. Either delete the matching relation row here or add/point to the reconciliation pass.

  **现有代码**

  ```
  113        self.group = [ref for ref in self.group if ref != self.ref_of(child)]
  114        self.save()
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#107** · `low/bug` · `L132` · `_link()` stores the child's block `oid` as the relation target, while `ref_of()` stores a `gid` for `GroupDat…
  <details><summary>详情与建议</summary>

  `_link()` stores the child's block `oid` as the relation target, while `ref_of()` stores a `gid` for `GroupData` children in `self.group`. The two membership stores therefore key group children by different identities: a consumer that takes the ref found in `group` (a `gid`) and queries `Relation.backlinks(...)`/`outbound(...)` for `contains` gets an empty result (the edge was written with the block oid), and `gid` values are themselves ULID-shaped, so such a lookup fails silently instead of erroring. Note also that when `child` is a `str` it is written verbatim into `dst`, which `Relation.target` then parses as an `Oid`. Pick one identity (block `oid` is what `Project.members` / `relation.target` expect) and use it in both stores, or document the mapping explicitly.

  **现有代码**

  ```
  132        target = str(child.oid) if isinstance(child, Block) else str(child)
  ```

  **建议**

  _（无）_

  </details>

## `src/feature/shared/provenance.py`

待办 0 / 共 1

- [x] **#93** · `high/bug` · `L50-L55` · Both `descendants` and `ancestors` never seed the origin `oid` into `seen` (only discovered children are added… ✅
  <details><summary>详情与建议</summary>

  Both `descendants` and `ancestors` never seed the origin `oid` into `seen` (only discovered children are added), so a cycle back to `oid` re-adds the origin to its own result: with edges A→B and B→A, `descendants(A)`/`ancestors(A)` return `(B, A)`, and a self-loop A→A returns `(A,)`, both violating the documented "all (recursively) derived from `oid`" contract. Seed the origin into the visited set (keep using the same string key you use for the frontier).

Secondary: `frontier.pop(0)` on a `list` shifts all remaining elements, making the BFS O(n²) on large derivation graphs; a `collections.deque` with `popleft()` keeps it linear (add `from collections import deque`).

  **现有代码**

  ```
  50    seen: set[str] = set()
  51    order: list[Oid] = []
  52    frontier = [str(oid)]
  53    while frontier:
  54        current = frontier.pop(0)
  55        for edge in Relation.backlinks(vault, current, relation=relation):
  ```

  **建议**

  ```
      origin = str(oid)
      seen: set[str] = {origin}
      order: list[Oid] = []
      frontier = deque([origin])
      while frontier:
          current = frontier.popleft()
          for edge in Relation.backlinks(vault, current, relation=relation):
  ```

  </details>

## `src/feature/shared/relation.py`

待办 3 / 共 5

- [x] **#98** · `medium/bug` · `L123-L128` · A bare `str` satisfies the declared `Iterable[str]` contract, so `tags="mytag"` is not stored as one tag but e… ✅
  <details><summary>详情与建议</summary>

  A bare `str` satisfies the declared `Iterable[str]` contract, so `tags="mytag"` is not stored as one tag but exploded into `{'m': None, 'y': None, ...}`. Guard the `str` case explicitly (or reject it) so a single tag string is handled as a single tag.

  **现有代码**

  ```
  123        if tags:
  124            attrs["tags"] = (
  125                {str(key): value for key, value in tags.items()}
  126                if isinstance(tags, Mapping)
  127                else {str(item): None for item in tags}
  128            )
  ```

  **建议**

  ```
          if tags:
              if isinstance(tags, Mapping):
                  attrs["tags"] = {str(key): value for key, value in tags.items()}
              elif isinstance(tags, str):
                  attrs["tags"] = {tags: None}
              else:
                  attrs["tags"] = {str(item): None for item in tags}
  ```

  </details>
- [x] **#100** · `medium/bug` · `L132-L133` · `source`/`target` are declared as `Oid | str` but are persisted with a plain `str()` and no validation or norm… ✅
  <details><summary>详情与建议</summary>

  `source`/`target` are declared as `Oid | str` but are persisted with a plain `str()` and no validation or normalization. Two problems: (1) a bogus id is written to the table and only blows up later in the `source`/`target` properties via `Oid.parse`; (2) `Oid.parse` upper-cases the id, but `create` does not, so a lower-case id is stored as-is and SQLite's case-sensitive TEXT comparison makes later `outbound`/`backlinks` lookups with the canonical (upper-case) `Oid` silently miss the row. Normalize through `Oid.parse` before writing.

  **现有代码**

  ```
  132                "src": str(source),
  133                "dst": str(target),
  ```

  **建议**

  ```
                  "src": str(Oid.parse(str(source))),
                  "dst": str(Oid.parse(str(target))),
  ```

  </details>
- [ ] **#102** · `medium/bug` · `L148-L149` · A missing row here means "not found", not a type mismatch —
  <details><summary>详情与建议</summary>

  A missing row here means "not found", not a type mismatch — raising `KindMismatchError` for an id that simply does not exist (or that refers to a different object such as a note) misleads callers that try to distinguish "absent" from "wrong kind". `core.types` already provides `ObjectNotFoundError` for this case.

  **现有代码**

  ```
  148        if not rows:
  149            raise KindMismatchError(f"{oid} 不是关系")
  ```

  **建议**

  ```
          if not rows:
              raise ObjectNotFoundError(f"{oid} 不是关系")
  ```

  </details>
- [ ] **#99** · `low/maintainability` · `L101-L105` · `props()` / `tags()` only make a shallow copy, so nested values inside `_attrs` (e.g.
  <details><summary>详情与建议</summary>

  `props()` / `tags()` only make a shallow copy, so nested values inside `_attrs` (e.g. the `tags` dict produced by `create`) are still shared with the instance. A caller doing `rel.props()["tags"]["x"] = 1` silently mutates the instance's in-memory row state. Return a deep copy (or rebuild the nested dicts) so the internal state cannot be corrupted from outside.

  **现有代码**

  ```
  101    def props(self) -> dict[str, Any]:
  102        return dict(self._attrs)
  103
  104    def tags(self) -> dict[str, Any]:
  105        return dict(self._attrs.get("tags") or {})
  ```

  **建议**

  ```
      def props(self) -> dict[str, Any]:
          return copy.deepcopy(self._attrs)
  
      def tags(self) -> dict[str, Any]:
          return copy.deepcopy(self._attrs.get("tags") or {})
  ```

  </details>
- [ ] **#101** · `low/performance` · `L181-L184` · `Table.select` accepts several equality filters, but the `relation` filter is applied in Python after fetching…
  <details><summary>详情与建议</summary>

  `Table.select` accepts several equality filters, but the `relation` filter is applied in Python after fetching and decoding every row for the given `src`/`dst`. Pushing `kind` into the WHERE clause avoids decoding/allocating `Relation` objects for rows that will be discarded and lets the engine use an index on `kind`.

  **现有代码**

  ```
  181        for row in _table(vault).select(src=str(source)):
  182            item = cls._from_row(vault, row)
  183            if relation is None or item.relation == relation:
  184                yield item
  ```

  **建议**

  ```
          where: dict[str, Any] = {"src": str(source)}
          if relation is not None:
              where["kind"] = relation
          for row in _table(vault).select(**where):
              yield cls._from_row(vault, row)
  ```

  </details>

## `src/feature/shared/signature.py`

待办 2 / 共 3

- [x] **#109** · `medium/bug` · `L61-L64` · `from_data` performs no validation/normalization, unlike the sibling `CanvasBody.from_data` (`if not data: ret… ✅
  <details><summary>详情与建议</summary>

  `from_data` performs no validation/normalization, unlike the sibling `CanvasBody.from_data` (`if not data: return cls()` + `data.get(...) or default`). Two consequences:
1. A falsy/non-mapping `data` (`None`, empty list, etc.) raises a confusing `TypeError` on `key in data` / `data[key]` instead of yielding an empty `Signature`.
2. Any field value is accepted verbatim, so a tampered stored record such as `{"created": null}` produces a `Signature` whose `verify()` raises `TypeError` from `int(created)` instead of failing closed (`False`). Since this class exists to detect tampering of stored data, it should not blow up on a malformed record.
Suggest guarding falsy input and coercing/validating the field values (e.g. `created=int(data.get("created") or 0)`, `str(...)` for the text fields).

  **现有代码**

  ```
  61        if isinstance(data, str):  # 兼容旧的弱签名串
  62            return cls(value=str(data))
  63        known = {key: data[key] for key in cls.__dataclass_fields__ if key in data}
  64        return cls(**known)
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#110** · `low/bug` · `L38` · `int(created)` silently truncates, so distinct inputs collapse to the same digest: `Signature.create(..., crea…
  <details><summary>详情与建议</summary>

  `int(created)` silently truncates, so distinct inputs collapse to the same digest: `Signature.create(..., created=1000.5)`, `created="1000"` and `created=1000` all produce the same `value`. Because `verify()` re-applies the same `int()` normalization, a record whose `created` field was changed to `1000.9` (or to the string `"1000"`) still verifies successfully, contradicting the module contract that "changing any field breaks the match". Keep the value type-strict instead of coercing it here (validate once on entry and digest the raw field).

  **现有代码**

  ```
  38        "created": int(created),
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#111** · `low/bug` · `L92-L94` · The dotted encoding is not unambiguous: fields are joined with `.` without escaping or length-prefixing, while…
  <details><summary>详情与建议</summary>

  The dotted encoding is not unambiguous: fields are joined with `.` without escaping or length-prefixing, while `author` is free-form user input and `alg`/`prev`/`subject`/`value` are also strings. An author containing `.` (or a `prev`/`subject` carrying a separator) makes the composite collide with a different field tuple, e.g. `("a.b", ...)` vs `("a", "b", ...)`, so the claimed self-contained encoding cannot be parsed back reliably. Either escape the separator (percent/backslash encoding) or emit a length-prefixed / canonical form; add a matching decoder to make the round-trip property testable.

  **现有代码**

  ```
  92        return ".".join(
  93            (FORMAT, self.alg, self.author, str(self.created), self.prev, self.subject, self.value)
  94        )
  ```

  **建议**

  _（无）_

  </details>

# 模块 `ui_tools`

## `src/ui_tools/component/__init__.py`

待办 1 / 共 1

- [ ] **#97** · `low/maintainability` · `L8` · Re-exporting the component `List` (and listing it in `__all__`) rebinds the name `List` at this package level.
  <details><summary>详情与建议</summary>

  Re-exporting the component `List` (and listing it in `__all__`) rebinds the name `List` at this package level. Consumers doing `import ui_tools.component as c` and then using `c.List[...]` as a type argument, or `from ui_tools.component import *` followed by a bare `List[...]` annotation, will silently pick up the widget class instead of `typing.List`, producing wrong annotations / runtime errors at evaluation time (e.g. with `typing.get_type_hints`). Consider exposing the widget under a distinct public alias (keeping the original name as a deprecated alias for existing callers) or, at minimum, documenting that `typing.List` must be imported explicitly in modules that also import this package's `List`.

  **现有代码**

  ```
  8from .atoms import Button, Chip, Divider, Field, Heading, Label, List
  ```

  **建议**

  _（无）_

  </details>

## `src/ui_tools/component/atoms.py`

待办 2 / 共 3

- [ ] **#134** · `medium/bug` · `L25-L28` · `align` and `thickness` are declared in `STYLABLE` but neither is a QSS property with a consumer: `Theme.to_qs…
  <details><summary>详情与建议</summary>

  `align` and `thickness` are declared in `STYLABLE` but neither is a QSS property with a consumer: `Theme.to_qss()` maps only `radius`/`border_color`/`font_size`/`font_weight`/`font_family` and otherwise emits `key.replace("_", "-")`, so `align: center;` / `thickness: 1px;` are written verbatim and silently dropped by Qt; `_label`/`_divider` in `ui_tools/core/qt.py` never read them. A theme rule using them is a silent no-op, contradicting the project rule that the theme vocabulary only covers what QSS can express. Either drop both entries or add explicit aliases (e.g. `align -> qproperty-alignment`, a QSS-expressible divider property such as `height`/`border_top_width`). The same `align` entry appears in `Heading` and has no consumer either.

  **现有代码**

  ```
  25    """文本标签。"""
  26
  27    kind = "label"
  28    STYLABLE = frozenset({"color", "font_size", "font_weight", "align"})
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#135** · `low/bug` · `L51` · `checked` cannot ever match: `_button` in `ui_tools/core/qt.py` creates a plain `QPushButton` without `setChec…
  <details><summary>详情与建议</summary>

  `checked` cannot ever match: `_button` in `ui_tools/core/qt.py` creates a plain `QPushButton` without `setCheckable(True)`, so the `checked` property stays `False` and a `widget.button:checked` rule is dead. Either make the translator build a checkable button (or a dedicated toggle atom, as the schema already has `ToggleSwitch`) or remove `checked` from this vocabulary to avoid a schema-valid but unreachable state.

  **现有代码**

  ```
  51    STATES = frozenset({"hover", "pressed", "disabled", "checked"})
  ```

  **建议**

  _（无）_

  </details>
- [x] **#136** · `low/documentation` · `L7` · Stale path in the module docstring: the Qt implementation lives at `ui_tools/core/qt.py` (`src/ui_tools/core/q… ✅
  <details><summary>详情与建议</summary>

  Stale path in the module docstring: the Qt implementation lives at `ui_tools/core/qt.py` (`src/ui_tools/core/qt.py`); `ui/core/qt.py` does not exist in this repository (the `ui` package was renamed to `ui_tools`), so the pointer misleads readers.

  **现有代码**

  ```
  7Qt 实现在 `ui/core/qt.py`；此处不依赖 Qt。
  ```

  **建议**

  _（无）_

  </details>

## `src/ui_tools/component/component.py`

待办 1 / 共 1

- [ ] **#112** · `low/maintainability` · `L21-L23` · `ui_signal` accepts any string, including `""` / whitespace.
  <details><summary>详情与建议</summary>

  `ui_signal` accepts any string, including `""` / whitespace. Signals are later resolved by `owner.widget` + `name` in `qt._connect`, which silently `return`s when the name is falsy (`if widget is None or not name: return`). So a typo/empty name produces a declaration that looks valid at build time but is silently dropped at connect time — hard to diagnose. Consider validating the name here and raising early (e.g. `UiError` from `..core.errors`) so the mistake surfaces at declaration time instead of being swallowed later.

  **现有代码**

  ```
  21    def ui_signal(self, name: str) -> UiSignal:
  22        """声明一条本部件的 UI 信号（`Bind` 的 source）。"""
  23        return UiSignal(name, owner=self)
  ```

  **建议**

  ```
      def ui_signal(self, name: str) -> UiSignal:
          """声明一条本部件的 UI 信号（`Bind` 的 source）。"""
          if not name or not name.strip():
              raise UiError(f"UI 信号名不能为空: {name!r}")
          return UiSignal(name, owner=self)
  ```

  </details>

## `src/ui_tools/component/structure.py`

待办 3 / 共 3

- [ ] **#113** · `medium/maintainability` · `L38` · `density` is an unvalidated free-form string, but the consumer only recognizes one specific literal: `src/ui_t…
  <details><summary>详情与建议</summary>

  `density` is an unvalidated free-form string, but the consumer only recognizes one specific literal: `src/ui_tools/core/cardview.py:287` does `self.set_dense(dense=density == "details")`. Any typo (`"detail"`, `"Details"`, `"card"`) silently degrades to the card grid with no error, and the docstring/parameter description never states that `"details"` is the magic value. Since this file is the public declaration surface, validate it here (or type it as `Literal["cards", "details"]` and document the accepted values) so a bad value fails fast instead of rendering the wrong density.

  **现有代码**

  ```
  38        density: str = "cards",
  ```

  **建议**

  ```
          density: Literal["cards", "details"] = "cards",
  ```

  </details>
- [ ] **#114** · `medium/maintainability` · `L61` · Same silent-fallback problem as `density`: `orientation` is stored verbatim and the Qt translator only tests f…
  <details><summary>详情与建议</summary>

  Same silent-fallback problem as `density`: `orientation` is stored verbatim and the Qt translator only tests for the single literal `"h"` (`src/ui_tools/core/qt.py:225`: `... if getattr(node, "orientation", "v") == "h" else QVBoxLayout(...)`). A typo such as `"H"`, `"horizontal"` or `"vert"` produces a vertical container without any diagnostic. Consider narrowing the type and/or raising a `LayoutError` for values outside the accepted set, and documenting the allowed values in the docstring.

  **现有代码**

  ```
  61        orientation: str = "v",
  ```

  **建议**

  ```
          orientation: Literal["v", "h"] = "v",
  ```

  </details>
- [ ] **#115** · `low/bug` · `L45` · Inconsistent default extractor: `title` falls back to `str` while `preview`/`meta`/`badge` fall back to `_empt…
  <details><summary>详情与建议</summary>

  Inconsistent default extractor: `title` falls back to `str` while `preview`/`meta`/`badge` fall back to `_empty`. For a model that can yield `None` (or an item whose text should be suppressed) the title paints the literal string `"None"` while the other fields render empty, which is a user-visible artifact rather than an obviously intended one. Either make the title extractor None-safe (e.g. `str` only for non-`None` items) or make it a required argument and document that the model contract excludes `None` items.

  **现有代码**

  ```
  45        self.title: Callable[[Any], str] = title or str
  ```

  **建议**

  _（无）_

  </details>

## `src/ui_tools/core/app.py`

待办 1 / 共 5

- [x] **#121** · `high/bug` · `L45-L47` · 同一个 `expects` 名被多个槽声明时，这里会把**同一个部件实例**填进每个槽。 ✅
  <details><summary>详情与建议</summary>

  同一个 `expects` 名被多个槽声明时，这里会把**同一个部件实例**填进每个槽。`Node.add` 会执行 `component._parent = self`，而一个节点只能有一个父节点，结果该部件被挂到多个槽下、`_parent` 只指向最后一个槽，`Node.path()` / 树结构随之错乱，且该部件会被重复渲染。建议把“一个部件只能填一个槽”作为硬约束，重复命中时报错，而不是静默重复填充。

  **现有代码**

  ```
  45        for slot in self.slots():
  46            if slot.expects is not None and slot.expects in parts:
  47                slot.add(parts[slot.expects])
  ```

  **建议**

  ```
          filled: set[str] = set()
          for slot in self.slots():
              if slot.expects is None or slot.expects not in parts:
                  continue
              if slot.expects in filled:
                  raise UiError(f"多个槽期待同一部件: {slot.expects!r}")
              slot.add(parts[slot.expects])
              filled.add(slot.expects)
  ```

  </details>
- [x] **#122** · `medium/bug` · `L44` · `add` 不是幂等的：同一个 facet 被重复挂载（或两个 facet 暴露同名部件）时，会再次 `slot.add(...)` 同一节点，槽里出现重复子件； ✅
  <details><summary>详情与建议</summary>

  `add` 不是幂等的：同一个 facet 被重复挂载（或两个 facet 暴露同名部件）时，会再次 `slot.add(...)` 同一节点，槽里出现重复子件；`_facets` 也会出现重复项，`schema()` 随之重复登记。建议增加幂等/冲突保护（例如重复 facet 直接返回，或按槽记录已填充的部件名）。

  **现有代码**

  ```
  44        parts = facet.parts()
  ```

  **建议**

  ```
          if facet in self._facets:
              return facet
          parts = facet.parts()
  ```

  </details>
- [x] **#123** · `medium/bug` · `L48` · 挂载不是原子的：填充循环中任意一个 `slot.add(...)` 抛出（锁死的槽 `addable=False`、或 `capacity` 已满都会抛 `LayoutError`）时，前面槽已经写入的部件不会回滚，而 … ✅
  <details><summary>详情与建议</summary>

  挂载不是原子的：填充循环中任意一个 `slot.add(...)` 抛出（锁死的槽 `addable=False`、或 `capacity` 已满都会抛 `LayoutError`）时，前面槽已经写入的部件不会回滚，而 `_facets.append` 尚未执行，App 处于“槽已填但 facet 未登记”的不一致状态，重试还会造成重复填充。建议先统一校验所有目标槽可填（addable / 容量），再执行写入；或在失败时回滚已写入的部件。

  **现有代码**

  ```
  48        self._facets.append(facet)
  ```

  **建议**

  _（无）_

  </details>
- [x] **#124** · `medium/bug` · `L58-L60` · `f"app.{rel}"` 未做重复检测即交给 `Schema.add`。 ✅
  <details><summary>详情与建议</summary>

  `f"app.{rel}"` 未做重复检测即交给 `Schema.add`。`Facet.name` 默认取 `type(domain).__name__`，两个同类型域（或显式同名）的 facet 会产生相同的相对路径，`Schema._nodes[path] = node` 会被后一个 facet 静默覆盖，导致路径解析到错误的节点且无任何提示。建议在编译 schema 时检测重复路径并报错。

  **现有代码**

  ```
  58        for facet in self._facets:
  59            for rel, node in facet.node_paths():
  60                schema.add(f"app.{rel}", node)
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#125** · `low/maintainability` · `L45` · 当没有任何槽的 `expects` 命中 `parts()` 的键（例如 App 作者拼写不一致）时，facet 仍会被登记但一个部件都没装上，且没有任何诊断信息，这类接线错误只能等到运行时界面空白才被发现。
  <details><summary>详情与建议</summary>

  当没有任何槽的 `expects` 命中 `parts()` 的键（例如 App 作者拼写不一致）时，facet 仍会被登记但一个部件都没装上，且没有任何诊断信息，这类接线错误只能等到运行时界面空白才被发现。建议对未匹配的部件名给出显式提示（日志或异常），至少覆盖 `parts()` 非空却零填充的情况。

  **现有代码**

  ```
  45        for slot in self.slots():
  ```

  **建议**

  _（无）_

  </details>

## `src/ui_tools/core/bind.py`

待办 2 / 共 2

- [ ] **#120** · `medium/bug` · `L35-L37` · `add()` appends unconditionally without deduplication.
  <details><summary>详情与建议</summary>

  `add()` appends unconditionally without deduplication. The consumer (`qt.py::_connect`) calls `signal.connect(binding.target)` directly, and Qt allows duplicate connections for the same (signal, slot) pair, so registering the identical (source, target) binding twice silently results in the handler being invoked twice. Consider deduplicating identical bindings here (or documenting that duplicates are intentional and filtering them at connect time).

  **现有代码**

  ```
  35        binding = Binding(source=source, target=target)
  36        self._items.append(binding)
  37        return binding
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#119** · `low/maintainability` · `L30` · `self._owner` is assigned but never read anywhere in `Bind` (no other member references `_owner`), so the stor…
  <details><summary>详情与建议</summary>

  `self._owner` is assigned but never read anywhere in `Bind` (no other member references `_owner`), so the stored owner is dead state. The module docstring claims the registry is scoped to its holder and released with it, but nothing here uses the owner for scoping/lifecycle. Either consume it (e.g. in `compile()`/`__repr__` or when resolving targets) or drop the attribute/parameter to avoid implying a behaviour that does not exist.

  **现有代码**

  ```
  30        self._owner = owner
  ```

  **建议**

  _（无）_

  </details>

## `src/ui_tools/core/bridge.py`

待办 0 / 共 1

- [x] **#130** · `medium/bug` · `L30-L31` · `Bridge` accepts a Qt `parent`, but `Session` keeps a strong reference to the bound `self._on_event` for the w… ✅
  <details><summary>详情与建议</summary>

  `Bridge` accepts a Qt `parent`, but `Session` keeps a strong reference to the bound `self._on_event` for the whole subscription lifetime. If the parented C++ `Bridge` is destroyed by Qt (e.g. its window/dialog is closed) without `close()` being called first, the Python wrapper stays referenced by `Session._observers`, and the next `Session` event reaches `self.changed.emit(event)` on an already-deleted QObject, raising `RuntimeError: Internal C++ object already deleted` (and keeping a dangling observer around). Consider wiring the Qt lifetime to the subscription, e.g. `self.destroyed.connect(lambda *_: self.close())` in `__init__`, so the observer is always detached when the C++ object goes away.

  **现有代码**

  ```
  30    def _on_event(self, event: object) -> None:
  31        self.changed.emit(event)
  ```

  **建议**

  _（无）_

  </details>

## `src/ui_tools/core/cardview.py`

待办 3 / 共 4

- [ ] **#144** · `medium/performance` · `L103` · `Model.items()` returns a fresh copy of the backing list (`return list(self._items)`), and `data()` calls it f…
  <details><summary>详情与建议</summary>

  `Model.items()` returns a fresh copy of the backing list (`return list(self._items)`), and `data()` calls it for **every** role lookup. Since a delegate paint queries DisplayRole, ROLE_PREVIEW, ROLE_META and ROLE_BADGE per item, each repainted item allocates four full copies of the list — O(4n) per visible row on the paint hot path. Cache the sequence (refreshed in `_reset`) or expose a non-copying accessor such as `__getitem__`/`index()` on `Model`.

  **现有代码**

  ```
  103        item = self._model.items()[index.row()]
  ```

  **建议**

  _（无）_

  </details>
- [x] **#145** · `medium/bug` · `L83` · `QtCardModel` registers a bound method as an observer on the long-lived `Model`, but `detach()` is never invok… ✅
  <details><summary>详情与建议</summary>

  `QtCardModel` registers a bound method as an observer on the long-lived `Model`, but `detach()` is never invoked anywhere in the codebase and `CardStageView` exposes no teardown hook. After the view is destroyed, the C++ object is deleted while `Model._observers` still holds the bound `_reset`; the next `Model` mutation then calls `beginResetModel()` on a deleted object (RuntimeError) and the reference also keeps the widget/model graph alive. Disconnect on destruction (e.g. connect `self.destroyed` to `detach`) instead of relying on garbage collection.

  **现有代码**

  ```
  83        self._cancel = model.watch(self._reset)
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#146** · `low/bug` · `L101-L102` · `isValid()` only proves the index is non-null; it does not prove `index.row()` is inside the current data.
  <details><summary>详情与建议</summary>

  `isValid()` only proves the index is non-null; it does not prove `index.row()` is inside the current data. The subscript below is unchecked, so any stale/persistent index that outlives a shrink of the underlying `Model` (e.g. a mutation occurring between a `rowCount()` query and `data()`) raises `IndexError` out of the paint path instead of returning `None`. Add a cheap bounds guard (`row < len(self._model)`) before indexing.

  **现有代码**

  ```
  101        if not index.isValid():
  102            return None
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#147** · `low/maintainability` · `L61` · The module docstring states colors are taken only from `Theme` tokens with no hardcoding, but this literal `#6…
  <details><summary>详情与建议</summary>

  The module docstring states colors are taken only from `Theme` tokens with no hardcoding, but this literal `#6C9BFF` violates that invariant. It is also redundant: `_BADGE_TOKENS.get(badge, "accent")` already falls back to the `accent` token. Drop the literal so unknown badges simply resolve through the token table.

  **现有代码**

  ```
  61    return QColor(theme.token(_BADGE_TOKENS.get(badge, "accent"), "#6C9BFF"))
  ```

  **建议**

  _（无）_

  </details>

## `src/ui_tools/core/compile.py`

待办 1 / 共 1

- [ ] **#129** · `medium/maintainability` · `L37-L44` · The translator is looked up only after the entire subtree has been compiled.
  <details><summary>详情与建议</summary>

  The translator is looked up only after the entire subtree has been compiled. Two consequences: (1) an unregistered `kind` at the root of a large tree still triggers compilation (and any translator side effects, e.g. `bind_widget` / widget creation in the Qt translators) for every descendant before failing; (2) the raised error comes from the deepest offending node while the message only prints `node.kind`, so diagnosing which branch was missing a translator requires guesswork. Resolve the handler first (fail fast) and include the node path in the error message.

  **现有代码**

  ```
  37        children: list[object] = []
  38        for placed in node.children():
  39            child = placed.component
  40            children.append(self.compile(child) if isinstance(child, Node) else child)
  41        handler = self._translators.get(node.kind)
  42        if handler is None:
  43            raise UiError(f"未注册的翻译器: {node.kind}")
  44        return handler(node, children)
  ```

  **建议**

  ```
          handler = self._translators.get(node.kind)
          if handler is None:
              raise UiError(f"未注册的翻译器: {node.path()} ({node.kind})")
          children: list[object] = []
          for placed in node.children():
              child = placed.component
              children.append(self.compile(child) if isinstance(child, Node) else child)
          return handler(node, children)
  ```

  </details>

## `src/ui_tools/core/conf.py`

待办 3 / 共 3

- [ ] **#127** · `medium/bug` · `L26-L29` · `add` is byte-for-byte identical to `set`, yet the module docstring defines `add` as "新增" (add a new key) whil…
  <details><summary>详情与建议</summary>

  `add` is byte-for-byte identical to `set`, yet the module docstring defines `add` as "新增" (add a new key) while `set` means "覆盖" (overwrite). Any caller relying on the documented add-new semantics will silently lose the existing value, with no error. Either implement distinct behavior (e.g. reject a duplicate key) or correct the documentation so the two methods are not advertised as different.

  **现有代码**

  ```
  26    def add(self, path: str, value: Any) -> ConfGroup:
  27        """新增一个键（封闭词表下与 `set` 同义，词表校验后续接 schema）。"""
  28        self._values[path] = value
  29        return self
  ```

  **建议**

  ```
      def add(self, path: str, value: Any) -> ConfGroup:
          """新增一个键；已存在时报错，避免静默覆盖。"""
          if path in self._values:
              raise KeyError(f"config key already exists: {path}")
          self._values[path] = value
          return self
  ```

  </details>
- [ ] **#128** · `low/maintainability` · `L17-L19` · `_closed` is set here but only ever read in `__repr__`; `set`/`add` apply no vocabulary check.
  <details><summary>详情与建议</summary>

  `_closed` is set here but only ever read in `__repr__`; `set`/`add` apply no vocabulary check. `theme` is declared as a closed vocabulary, so any invalid token is accepted silently and the closed contract cannot be relied upon. If enforcement is intentionally deferred, consider failing fast (e.g. reject keys not in an allowed set) or at least raising/documenting clearly so callers do not assume validation happens.

  **现有代码**

  ```
  17    def __init__(self, *, closed: bool = False) -> None:
  18        self._closed = closed
  19        self._values: dict[str, Any] = {}
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#126** · `low/maintainability` · `L34-L35` · `items()` returns only a shallow copy, so nested mutable values (lists/dicts stored via `set`/`add`) remain sh…
  <details><summary>详情与建议</summary>

  `items()` returns only a shallow copy, so nested mutable values (lists/dicts stored via `set`/`add`) remain shared with the group's internal state; a caller mutating them leaks changes back. The same applies to `get`, which returns the stored object by reference. If defensive copying is intended, use `copy.deepcopy`, otherwise document that returned containers are shared.

  **现有代码**

  ```
  34    def items(self) -> dict[str, Any]:
  35        return dict(self._values)
  ```

  **建议**

  ```
      def items(self) -> dict[str, Any]:
          return copy.deepcopy(self._values)
  ```

  </details>

## `src/ui_tools/core/config.py`

待办 3 / 共 3

- [ ] **#132** · `medium/bug` · `L33-L34` · The runtime shape of `data` is not validated: if `items` is not a mapping (e.g.
  <details><summary>详情与建议</summary>

  The runtime shape of `data` is not validated: if `items` is not a mapping (e.g. a scalar slipped into the config payload), `items.items()` raises `AttributeError` instead of the intended `UiError`, and the failure surfaces far from the caller's mistake. The type hint is not enforced at runtime, so add an explicit check and raise `UiError` with a clear message.

  **现有代码**

  ```
  33    for path, items in data.items():
  34        full = schema.resolve(path)
  ```

  **建议**

  ```
      for path, items in data.items():
          if not isinstance(items, Mapping):
              raise UiError(f"配置项必须是映射: {path!r} -> {type(items).__name__}")
          full = schema.resolve(path)
  ```

  </details>
- [ ] **#131** · `medium/bug` · `L38-L40` · Non-atomic application: validation of later paths happens *after* earlier nodes have already been mutated.
  <details><summary>详情与建议</summary>

  Non-atomic application: validation of later paths happens *after* earlier nodes have already been mutated. If `schema.resolve(path)` raises or `schema.node(full)` returns `None` for a subsequent entry, `UiError` propagates but the earlier `target.set(...)` calls are already committed, leaving the configuration half-applied (and callers naturally assume a raised `UiError` means nothing changed). Consider a two-pass approach: first resolve every path and collect `(target, item, value)` into a plan, and only then write.

  **现有代码**

  ```
  38        target = node.conf.theme if group == "theme" else node.conf.attr
  39        for item, value in items.items():
  40            target.set(item, value)
  ```

  **建议**

  ```
          target = node.conf.theme if group == "theme" else node.conf.attr
          for item, value in items.items():
              plan.append((target, item, value))
      for target, item, value in plan:
          target.set(item, value)
  ```

  </details>
- [ ] **#133** · `low/bug` · `L17` · Note on the runtime-mapping check suggested for `items`: `Mapping` is currently imported only under `TYPE_CHEC…
  <details><summary>详情与建议</summary>

  Note on the runtime-mapping check suggested for `items`: `Mapping` is currently imported only under `TYPE_CHECKING`, so `isinstance(items, Mapping)` would raise `NameError` at runtime. If the shape check is added, move this import out of the `if TYPE_CHECKING:` block (runtime `from collections.abc import Mapping`) or reference `collections.abc.Mapping` directly.

  **现有代码**

  ```
  17    from collections.abc import Mapping
  ```

  **建议**

  _（无）_

  </details>

## `src/ui_tools/core/facet.py`

待办 0 / 共 2

- [x] **#137** · `medium/bug` · `L57-L59` · `page()` 只写不清理，与文档承诺的「重复注册以最后一次为准」不符： - 同一个 `Page` 换路由重新注册时，旧路由仍留在 `_routes` 中（`navigate(旧路由)` 依然能反查到该页）； ✅
  <details><summary>详情与建议</summary>

  `page()` 只写不清理，与文档承诺的「重复注册以最后一次为准」不符：
- 同一个 `Page` 换路由重新注册时，旧路由仍留在 `_routes` 中（`navigate(旧路由)` 依然能反查到该页）；
- 同一个路由绑定到另一个 `Page` 时，旧 `Page` 仍留在 `_pages` 中，于是会继续参与 `pages()` / `compile_bindings()` / `node_paths()`，但永远无法被 `navigate` 到达。
建议在写入前清理反向/正向的陈旧条目。

  **现有代码**

  ```
  57        self._pages[page] = route
  58        self._routes[route] = page
  59        return page
  ```

  **建议**

  ```
          old_route = self._pages.get(page)
          if old_route is not None and old_route is not route:
              self._routes.pop(old_route, None)
          stale = self._routes.get(route)
          if stale is not None and stale is not page:
              self._pages.pop(stale, None)
          self._pages[page] = route
          self._routes[route] = page
          return page
  ```

  </details>
- [x] **#138** · `medium/bug` · `L76-L77` · `compile_bindings()` 先单独编译 `self.root.bind`，随后又遍历 `self._pages` 逐个编译； ✅
  <details><summary>详情与建议</summary>

  `compile_bindings()` 先单独编译 `self.root.bind`，随后又遍历 `self._pages` 逐个编译；一旦默认页自身被 `facet.page(facet.root, ...)` 注册（`Page` 本身就是合法注册对象），`root` 的绑定会被编译两次，返回列表中出现重复 `Binding`，下游按列表连接时会把同一信号重复连到同一目标。建议循环时跳过 `self.root`。

  **现有代码**

  ```
  76        for page in self._pages:
  77            result.extend(page.bind.compile())
  ```

  **建议**

  ```
          for page in self._pages:
              if page is self.root:
                  continue
              result.extend(page.bind.compile())
  ```

  </details>

## `src/ui_tools/core/model.py`

待办 3 / 共 3

- [ ] **#140** · `medium/bug` · `L59-L61` · `watch` appends without de-duplication, yet `cancel` removes only the first matching entry.
  <details><summary>详情与建议</summary>

  `watch` appends without de-duplication, yet `cancel` removes only the first matching entry. If the same callback is registered twice (e.g. a component is re-watched without cancelling first), it receives every change twice and the returned cancel function silently leaves the second registration alive — the caller believes it detached while notifications keep arriving (the same pattern exists in `session.py`). De-duplicate on registration or make `cancel` remove all matching entries.

  **现有代码**

  ```
  59        def cancel() -> None:
  60            if callback in self._observers:
  61                self._observers.remove(callback)
  ```

  **建议**

  ```
          def cancel() -> None:
              self._observers[:] = [cb for cb in self._observers if cb is not callback]
  ```

  </details>
- [ ] **#139** · `medium/bug` · `L65-L67` · `_notify()` invokes observers with no isolation.
  <details><summary>详情与建议</summary>

  `_notify()` invokes observers with no isolation. Since `append`/`replace`/`remove`/`clear` mutate `_items` *before* notifying, a single raising observer (a) aborts the notification loop so all later observers — e.g. a `QtListModel.reset` or a `Session` projection — silently miss the change and stay stale against already-mutated data, and (b) propagates the exception out of a mutator the caller thinks succeeded. Either isolate each callback (log and continue) or document the fail-fast contract explicitly.

  **现有代码**

  ```
  65    def _notify(self) -> None:
  66        for callback in list(self._observers):
  67            callback()
  ```

  **建议**

  ```
      def _notify(self) -> None:
          for callback in list(self._observers):
              try:
                  callback()
              except Exception:  # noqa: BLE001 — 单个观察者失败不应中断其余通知
                  _log.exception("model observer failed")
  ```

  </details>
- [ ] **#141** · `low/performance` · `L28-L31` · `replace` (and likewise `clear`, which notifies even when the list is already empty) always fires `_notify()` …
  <details><summary>详情与建议</summary>

  `replace` (and likewise `clear`, which notifies even when the list is already empty) always fires `_notify()` regardless of whether the contents actually changed. `Session._on_event` calls `model.replace(loader())` for every registered model on every event, so unrelated events trigger a full obs. observer round-trip per model — the Qt bridge turns each into a `beginResetModel`/`endResetModel` full view reset. Skipping the notification when the new contents equal the current ones avoids the redundant refresh.

  **现有代码**

  ```
  28    def replace(self, items: Iterable[T]) -> None:
  29        """整体替换并通知。"""
  30        self._items = list(items)
  31        self._notify()
  ```

  **建议**

  ```
      def replace(self, items: Iterable[T]) -> None:
          """整体替换并通知（内容一致时跳过）。"""
          new_items = list(items)
          if new_items == self._items:
              return
          self._items = new_items
          self._notify()
  ```

  </details>

## `src/ui_tools/core/node.py`

待办 2 / 共 4

- [x] **#151** · `medium/bug` · `L138-L139` · `add()` overwrites `_parent` without detaching the node from its previous parent, leaving the same node presen… ✅
  <details><summary>详情与建议</summary>

  `add()` overwrites `_parent` without detaching the node from its previous parent, leaving the same node present in two parents' `_children`. It will then be traversed twice by `walk()` and compiled twice (two widget instances for one node), while `path()` reports only the newest ancestry. Detach from the old parent (or raise on re-parenting) before appending.

  **现有代码**

  ```
  138        if isinstance(component, Node):
  139            component._parent = self  # noqa: SLF001 — 建树，同模块强耦合
  ```

  **建议**

  ```
          if isinstance(component, Node):
              old = component._parent  # noqa: SLF001 — 建树，同模块强耦合
              if old is not None and old is not self:
                  old._children = [p for p in old._children if p.component is not component]  # noqa: SLF001
              component._parent = self  # noqa: SLF001 — 建树，同模块强耦合
  ```

  </details>
- [x] **#154** · `medium/bug` · `L147-L149` · `clear()` drops the children but never resets their `_parent`, so removed nodes keep pointing at this node. ✅
  <details><summary>详情与建议</summary>

  `clear()` drops the children but never resets their `_parent`, so removed nodes keep pointing at this node. `Page.set()` relies on `clear()`, meaning the discarded layout still reports this page as its parent, and a later `path()` on the detached subtree yields a stale path (used for style selectors). Reset `_parent` on the removed children.

  **现有代码**

  ```
  147    def clear(self) -> None:
  148        """清空子件。"""
  149        self._children.clear()
  ```

  **建议**

  ```
      def clear(self) -> None:
          """清空子件。"""
          for placed in self._children:
              if isinstance(placed.component, Node):
                  placed.component._parent = None  # noqa: SLF001 — 拆树，同模块强耦合
          self._children.clear()
  ```

  </details>
- [ ] **#152** · `medium/bug` · `L43-L46` · `__call__` accepts `*_args, **_kwargs` but always invokes `handler()` with no arguments, so any signal payload…
  <details><summary>详情与建议</summary>

  `__call__` accepts `*_args, **_kwargs` but always invokes `handler()` with no arguments, so any signal payload (e.g. Qt's `clicked(checked: bool)`) is silently dropped. A target widget method that declares parameters (e.g. `set_density(self, on: bool)`) will therefore always be called with none — either a `TypeError` or silently wrong behavior that is hard to trace. Either forward the payload when the handler accepts parameters, or drop the varargs from the signature and document explicitly that payloads are never forwarded.

  **现有代码**

  ```
  43    def __call__(self, *_args: object, **_kwargs: object) -> None:
  44        handler = getattr(self._node.widget, self._method, None)
  45        if callable(handler):
  46            handler()
  ```

  **建议**

  ```
      def __call__(self, *args: object, **kwargs: object) -> None:
          handler = getattr(self._node.widget, self._method, None)
          if not callable(handler):
              return
          try:
              signature = inspect.signature(handler)
          except (TypeError, ValueError):
              signature = None
          if signature is not None and len(signature.parameters) == 0:
              handler()  # 无参槽：忽略信号载荷
          else:
              handler(*args, **kwargs)
  ```

  </details>
- [ ] **#153** · `medium/maintainability` · `L45-L46` · A missing widget (node never compiled, `widget is None`) or a misspelled/absent method name makes the action a…
  <details><summary>详情与建议</summary>

  A missing widget (node never compiled, `widget is None`) or a misspelled/absent method name makes the action a silent no-op, so a broken UI→UI binding fails only at user interaction time with no diagnostic. Consider raising (e.g. `LayoutError`/`UiError`) when the node is compiled but the named method is missing, at least in a strict/verbose mode.

  **现有代码**

  ```
  45        if callable(handler):
  46            handler()
  ```

  **建议**

  _（无）_

  </details>

## `src/ui_tools/core/qt.py`

待办 4 / 共 5

- [ ] **#169** · `medium/maintainability` · `L121-L122` · Broken bindings disappear silently.
  <details><summary>详情与建议</summary>

  Broken bindings disappear silently. `Bind.compile()` only validates that the source is a `UiSignal` and the target is callable; it does not check that the owning node was actually compiled or that the signal name exists on the target widget. So a typo in `ui_signal("clicked")`/`ui_signal("textChanged")`, or a node that ended up outside the compiled tree (e.g. a `stage` child that is discarded), yields a dead control with no error and no log, which is very hard to diagnose. Consider raising `UiError` (or at least logging) when `widget is not None` but the named signal cannot be resolved.

  **现有代码**

  ```
  121        if widget is None or not name:
  122            return
  ```

  **建议**

  _（无）_

  </details>
- [x] **#168** · `medium/bug` · `L212` · Scroll slot loses its declared stretch. ✅
  <details><summary>详情与建议</summary>

  Scroll slot loses its declared stretch. `_tag(widget, node)` writes `cairnStretch` onto the *inner* widget, but when `scroll=True` the object returned to the parent layout is the `QScrollArea`, which never gets `cairnStretch`. Since `_weight()` only reads `widget.property("cairnStretch")`, a slot declared with `stretch=True, scroll=True` silently gets weight 0 and collapses instead of expanding. Also note `node.bind_widget(area)` overwrites the binding done inside `_tag`, so the node's widget reference changes underneath any already-created handle.

  **现有代码**

  ```
  212        area.setProperty("cairnClass", "scroll")
  ```

  **建议**

  ```
          area.setProperty("cairnClass", "scroll")
          area.setProperty("cairnStretch", node.stretch)
  ```

  </details>
- [ ] **#172** · `low/bug` · `L166` · `int(theme.token(...))` assumes the token is an integer literal, but theme tokens are schema-validated only as…
  <details><summary>详情与建议</summary>

  `int(theme.token(...))` assumes the token is an integer literal, but theme tokens are schema-validated only as string/number/boolean (`schema/theme.json`), so a perfectly schema-valid theme such as `"shadow_alpha": "0.5"` or `"110.0"` breaks window construction with a bare `ValueError` and no indication of which token is wrong. Wrap the conversion (or clamp/fall back to the documented default) and report the offending token on failure.

  **现有代码**

  ```
  166    color.setAlpha(int(theme.token("shadow_alpha", "110")))
  ```

  **建议**

  ```
      try:
          alpha = int(theme.token("shadow_alpha", "110"))
      except ValueError as err:
          raise UiError(f"shadow_alpha 不是整数: {theme.token('shadow_alpha')!r}") from err
      color.setAlpha(alpha)
  ```

  </details>
- [ ] **#170** · `low/maintainability` · `L238` · `del children` silently throws away the compiled child widgets of a `stage` node.
  <details><summary>详情与建议</summary>

  `del children` silently throws away the compiled child widgets of a `stage` node. The children have already been compiled by `Compiler.compile`, so their nodes have been `bind_widget`-ed to widgets that are then garbage-collected (no parent, no reference) — any binding/`NodeAction` pointing at them will later fail with a deleted-C++-object error instead of a clear message. Silently ignoring declared children also hides a structural mistake in the declaration tree; prefer raising `UiError` when children are present (or document the restriction explicitly).

  **现有代码**

  ```
  238    del children
  ```

  **建议**

  ```
      if children:
          raise UiError("stage 不支持子件")
      del children
  ```

  </details>
- [ ] **#171** · `low/bug` · `L257` · `cols` is taken from raw options with no validation.
  <details><summary>详情与建议</summary>

  `cols` is taken from raw options with no validation. `Grid("g", cols=-1)` (or `cols=-2`) passes the `or 1` guard and then produces negative row indices (`1 // -1 == -1`) while `% -1` is always 0, so every child lands in column 0 on an invalid row and the grid renders incorrectly/at Qt's mercy. A non-numeric value (e.g. `cols="two"`) also escapes as a bare `ValueError` rather than a `UiError`. Validate the value before use.

  **现有代码**

  ```
  257    cols = int(node.options.get("cols", 1) or 1)
  ```

  **建议**

  ```
      cols = int(node.options.get("cols", 1) or 1)
      if cols < 1:
          raise UiError(f"grid 的 cols 必须为正数: {cols}")
  ```

  </details>

## `src/ui_tools/core/qtmodel.py`

待办 1 / 共 2

- [ ] **#142** · `medium/performance` · `L61` · `data()` is called once per visible cell on every paint/scroll, and each call materializes a full copy of the …
  <details><summary>详情与建议</summary>

  `data()` is called once per visible cell on every paint/scroll, and each call materializes a full copy of the underlying list (`Model.items()` returns `list(self._items)`), making every repaint O(rows) allocations on a hot GUI path. It also indexes the copy with `index.row()` without any bounds check, so a stale `QModelIndex`/`QPersistentModelIndex` (model changed or reset after the view queried `rowCount()`) raises `IndexError` inside Qt's paint path. Prefer a bounds-checked lookup and cache/lookup directly instead of re-copying per cell (e.g. add an indexed accessor to `Model`, or keep a cached snapshot refreshed in `_reset()`).

  **现有代码**

  ```
  61        return self._row(self._model.items()[index.row()])
  ```

  **建议**

  ```
          row = index.row()
          items = self._model.items()
          if not 0 <= row < len(items):
              return None
          return self._row(items[row])
  ```

  </details>
- [x] **#143** · `medium/bug` · `L63-L65` · The subscription is only released when a caller explicitly invokes `detach()`. ✅
  <details><summary>详情与建议</summary>

  The subscription is only released when a caller explicitly invokes `detach()`. `Model.watch` stores the bound method `self._reset` in `Model._observers`, which (a) keeps this `QtListModel` Python wrapper alive for as long as the `Model` lives, and (b) if the underlying C++ `QObject` has already been deleted (e.g. the view/model was destroyed without calling `detach()`), a later notification invokes `_reset` on a deleted C++ object and raises `RuntimeError: Internal C++ object already deleted`. Hook the Qt destruction signal so the observer is removed automatically, e.g. in `__init__`: `self.destroyed.connect(lambda *_: self.detach())` (or override the QObject destruction path).

  **现有代码**

  ```
  63    def detach(self) -> None:
  64        """断开对 `Model` 的观察。"""
  65        self._cancel()
  ```

  **建议**

  _（无）_

  </details>

## `src/ui_tools/core/schema.py`

待办 3 / 共 3

- [ ] **#148** · `medium/bug` · `L35-L37` · The docstring promises `None` for an unaddressable path, but `resolve()` raises `UiError` when the path is not…
  <details><summary>详情与建议</summary>

  The docstring promises `None` for an unaddressable path, but `resolve()` raises `UiError` when the path is not registered at all, so `node("unknown.x")` raises instead of returning `None`. Callers that use `node()` as an existence probe (`config.py` only guards against `None`) will get an unexpected exception. Either catch the lookup failure here and return `None`, or correct the docstring to state that an unknown path raises.

  **现有代码**

  ```
  35    def node(self, path: str) -> Node | None:
  36        """解析路径并返回其节点（不可寻址返回 `None`）。"""
  37        return self._nodes.get(self.resolve(path))
  ```

  **建议**

  ```
      def node(self, path: str) -> Node | None:
          """解析路径并返回其节点（不可寻址或未登记返回 `None`）。"""
          try:
              full = self.resolve(path)
          except UiError:
              return None
          return self._nodes.get(full)
  ```

  </details>
- [ ] **#149** · `medium/bug` · `L61-L62` · `__contains__` only applies `normalize()`, while `resolve()` also accepts suffix-completed forms.
  <details><summary>详情与建议</summary>

  `__contains__` only applies `normalize()`, while `resolve()` also accepts suffix-completed forms. As a result `"note.edit.grid.editor" in schema` is `False` even though `resolve()`/`node()` handle that same input successfully — an asymmetry that makes membership checks silently disagree with resolution (and breaks the usual `if p not in schema: raise` guard pattern for domain-rooted paths). Reuse `resolve()` here via a `UiError` guard so both APIs share one contract.

  **现有代码**

  ```
  61    def __contains__(self, path: object) -> bool:
  62        return isinstance(path, str) and self.normalize(path) in self._paths
  ```

  **建议**

  ```
      def __contains__(self, path: object) -> bool:
          if not isinstance(path, str):
              return False
          try:
              self.resolve(path)
          except UiError:
              return False
          return True
  ```

  </details>
- [ ] **#150** · `low/performance` · `L54` · Every `resolve()` call scans and sorts the entire registered path set.
  <details><summary>详情与建议</summary>

  Every `resolve()` call scans and sorts the entire registered path set. Since `apply_config` invokes `resolve()` once per config entry, resolving m entries over n registered paths costs O(m · n log n) even though the registry never changes during a load. Caching the sorted path list on `add()` (or grouping candidates by their last segment) keeps lookups cheap as the Facet tree grows.

  **现有代码**

  ```
  54        matches = sorted(p for p in self._paths if p == path or p.endswith(f".{path}"))
  ```

  **建议**

  _（无）_

  </details>

## `src/ui_tools/core/session.py`

待办 4 / 共 4

- [ ] **#160** · `medium/maintainability` · `L65-L68` · `close()` does not mark the session as terminal.
  <details><summary>详情与建议</summary>

  `close()` does not mark the session as terminal. After it runs, `watch()`, `projection()` and `model()` remain fully callable: they append to `_observers` / `_models` and fill `_cache`, but the backbone subscription is already cancelled, so those observers/models will never be notified and the projections will serve stale values indefinitely (the cache is never invalidated again). Add a `_closed` flag and guard the mutating entry points (raise or return a no-op) so use-after-close fails loudly instead of silently going dead.

  **现有代码**

  ```
  65        self._sub.cancel()
  66        self._observers.clear()
  67        self._cache.clear()
  68        self._models.clear()
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#158** · `medium/bug` · `L71-L75` · `_on_event` refreshes models and dispatches to observers without per-item exception isolation.
  <details><summary>详情与建议</summary>

  `_on_event` refreshes models and dispatches to observers without per-item exception isolation. If one `loader()` (or one observer callback) raises, the remaining models are never refreshed and the remaining observers are silently skipped — even though `_cache.clear()` has already run, so the projections are invalidated but nothing is rebuilt. The `EventBus.emit` guard only isolates the exception at the bus level (it logs and continues to other subscribers); it does not resume the work interrupted inside this handler. Wrap each `model.replace(loader())` and each `callback(event)` in its own `try/except Exception` (logging via a module-level `logging.getLogger(__name__)`) so a single faulty loader/observer cannot leave the session in a half-refreshed state.

  **现有代码**

  ```
  71        self._cache.clear()
  72        for loader, model in self._models:
  73            model.replace(loader())
  74        for callback in list(self._observers):
  75            callback(event)
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#159** · `low/bug` · `L45-L47` · `cancel()` removes only one occurrence of `callback`.
  <details><summary>详情与建议</summary>

  `cancel()` removes only one occurrence of `callback`. If the same callback is registered twice, a single `cancel()` leaves the second registration active, so the caller believes it unsubscribed while it still receives events. Either reject duplicate registration, or make `cancel()` remove all matching entries (e.g. `self._observers = [o for o in self._observers if o is not callback]`).

  **现有代码**

  ```
  45        def cancel() -> None:
  46            if callback in self._observers:
  47                self._observers.remove(callback)
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#161** · `low/maintainability` · `L59-L60` · `model()` registers the model in `_models` with no corresponding deregistration API —
  <details><summary>详情与建议</summary>

  `model()` registers the model in `_models` with no corresponding deregistration API — the only way to drop a model is `close()`, which tears down the whole session. Any model created and discarded (e.g. per-panel or per-dialog) stays strongly referenced and keeps being recomputed on every backbone event, which is both a leak and wasted work. Consider returning a handle with a `release()`/`cancel()` that removes the `(loader, model)` entry.

  **现有代码**

  ```
  59        model: Model[T] = Model(loader())
  60        self._models.append((loader, model))
  ```

  **建议**

  _（无）_

  </details>

## `src/ui_tools/core/show.py`

待办 3 / 共 3

- [ ] **#174** · `medium/bug` · `L121` · When more than one input unit shares the same type, `groups[type]` repeats every field name of that type (two …
  <details><summary>详情与建议</summary>

  When more than one input unit shares the same type, `groups[type]` repeats every field name of that type (two NoteData units yield each field twice), and the same non-deduplicating accumulation applies to `self.ids`. A consumer that walks `groups` to enumerate fields will render duplicates. Deduplicate while preserving first-seen order (and consider the same for `ids`, which the docstring describes as a union).

  **现有代码**

  ```
  121            self.groups.setdefault(part.type, []).extend(part.attrs)  # L1：按来源类型分组
  ```

  **建议**

  ```
              group = self.groups.setdefault(part.type, [])
              group.extend(name for name in part.attrs if name not in group)
  ```

  </details>
- [ ] **#173** · `medium/bug` · `L40-L42` · `_one` only expands `list`/`tuple`;
  <details><summary>详情与建议</summary>

  `_one` only expands `list`/`tuple`; any other iterable of units (a `set`, generator, `dict.values()`, a domain service that yields units, ...) is silently treated as a **single** data unit, so `Show(...)` produces one bogus part (type taken from the container) instead of one part per unit — a silent wrong result rather than an error. Consider accepting any non-`str`/`bytes`/`Mapping` `Iterable` (both are already imported) or raising a clear error for unexpected container types.

  **现有代码**

  ```
  40    if isinstance(value, (list, tuple)):
  41        return list(value)
  42    return [value]
  ```

  **建议**

  ```
      if isinstance(value, Iterable) and not isinstance(value, (str, bytes, Mapping)):
          return list(value)
      return [value]
  ```

  </details>
- [ ] **#175** · `-/other` · `L107` · `Show` is a `@dataclass` but also defines its own `__init__`, so the `field(default_factory=...)` defaults dec…
  <details><summary>详情与建议</summary>

  `Show` is a `@dataclass` but also defines its own `__init__`, so the `field(default_factory=...)` defaults declared here are never used (only the auto-generated `__repr__`/`__eq__` read the fields). Any field added later to the dataclass body will be initialized nowhere — `__init__` won't set it and the generated `__eq__`/`__repr__` will read a missing attribute, failing at runtime instead of at definition time. Either drop `@dataclass`, or keep the dataclass and move the L0/L1 merge into `__post_init__` so initialization stays in one place.

  **现有代码**

  ```
  107    parts: list[ShowPart] = field(default_factory=list)
  ```

  **建议**

  _（无）_

  </details>

## `src/ui_tools/core/slot.py`

待办 2 / 共 3

- [x] **#156** · `medium/bug` · `L35` · `**opts` is forwarded to `Node.__init__` together with hard-coded `addable`, `capacity` and `stretch`. ✅
  <details><summary>详情与建议</summary>

  `**opts` is forwarded to `Node.__init__` together with hard-coded `addable`, `capacity` and `stretch`. `addable` is not an explicit parameter of `Slot`, so a caller writing `Slot("x", addable=False)` (a legitimate `Node` capability) has it collected into `opts` and then triggers `TypeError: got multiple values for keyword argument 'addable'` from the `super().__init__` call. Either expose `addable` explicitly (e.g. default `None` meaning "derive from `locked`") or pop/resolve it from `opts` before delegating.

  **现有代码**

  ```
  35        super().__init__(name, addable=not locked, capacity=capacity, stretch=stretch, **opts)
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#157** · `low/maintainability` · `L39` · `addable` is derived once at construction (`addable=not locked`) while `locked` is stored as an independent at…
  <details><summary>详情与建议</summary>

  `addable` is derived once at construction (`addable=not locked`) while `locked` is stored as an independent attribute, creating two sources of truth for the same state. Any later mutation of `slot.locked` leaves `Node.addable` inconsistent with the declared lock (and `self.locked` is not read anywhere in production code, only fed as a constructor input), so the derived flag is the only one that actually enforces behaviour. Consider making `locked` a property computed from `addable` (or vice versa) so the two cannot diverge.

  **现有代码**

  ```
  39        self.locked = locked
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#155** · `low/maintainability` · `L40` · `align` (like `capacity`) accepts any string/int with no validation, and the consumer only recognises a single…
  <details><summary>详情与建议</summary>

  `align` (like `capacity`) accepts any string/int with no validation, and the consumer only recognises a single literal (`node.align == "top"` in the Qt backend), so typos such as `align="Top"` are silently treated as the default instead of failing fast. Consider typing it as `Literal["top"] | None` or validating the value in `__init__` so an invalid alignment surfaces at declaration time.

  **现有代码**

  ```
  40        self.align = align
  ```

  **建议**

  _（无）_

  </details>

## `src/ui_tools/core/theme.py`

待办 4 / 共 4

- [ ] **#162** · `medium/bug` · `L60-L61` · Unresolved `token.*` references fail silently: when the key is missing, `token()` falls back to the raw `"toke…
  <details><summary>详情与建议</summary>

  Unresolved `token.*` references fail silently: when the key is missing, `token()` falls back to the raw `"token.xxx"` string and that literal is written straight into the QSS. Qt then silently drops the declaration (invalid value) instead of complaining, so a typo such as `token.acccent` (or a token renamed in the JSON) results in a widget that is quietly unstyled with no diagnostic. Since `token` is documented as the single source of truth, a dangling reference should be surfaced (raise, or at minimum log a warning naming the key and the owning style).

  **现有代码**

  ```
  60        if value.startswith(_REF_PREFIX):
  61            return self.token(value[len(_REF_PREFIX) :], value)
  ```

  **建议**

  ```
          if value.startswith(_REF_PREFIX):
              key = value[len(_REF_PREFIX) :]
              if key not in self._tokens:
                  raise KeyError(f"unknown token reference: {value!r}")
              return self._tokens[key]
  ```

  </details>
- [ ] **#164** · `medium/bug` · `L93-L95` · The JSON shape is assumed, not validated: a theme file whose root is not an object (e.g.
  <details><summary>详情与建议</summary>

  The JSON shape is assumed, not validated: a theme file whose root is not an object (e.g. a JSON array) fails at `data.get`, and `"token"`/`"style"` entries that are not objects (e.g. a string or list) blow up later inside `Theme.__init__` with `.items()`. Because `load_theme` reads user-editable files from `CAIRN_THEME_DIR`, malformed input should produce a clear error mentioning the offending path/section instead of an opaque `AttributeError`/`TypeError` with no file context.

  **现有代码**

  ```
  93    data: dict[str, Any] = json.loads(Path(path).read_text(encoding="utf-8"))
  94    tokens = data.get("token") or {}
  95    styles = data.get("style") or {}
  ```

  **建议**

  ```
      data = json.loads(Path(path).read_text(encoding="utf-8"))
      if not isinstance(data, dict):
          raise ValueError(f"theme file must be a JSON object: {path}")
      tokens = data.get("token") or {}
      styles = data.get("style") or {}
      if not isinstance(tokens, dict) or not isinstance(styles, dict):
          raise ValueError(f"'token' and 'style' must be objects in {path}")
      return Theme(tokens, styles)
  ```

  </details>
- [ ] **#165** · `low/bug` · `L38` · `str(v)` silently converts every value type, so `null` becomes the literal `None` and a JSON boolean becomes `…
  <details><summary>详情与建议</summary>

  `str(v)` silently converts every value type, so `null` becomes the literal `None` and a JSON boolean becomes `True`/`False` in the emitted QSS (neither is a valid declaration, and Qt drops them without warning). Since the loader performs no schema validation, consider rejecting/ignoring `None` and `bool` values (only `str`/`int`/`float` should be coerced) so bad configuration surfaces early.

  **现有代码**

  ```
  38        self._tokens: dict[str, str] = {str(k): str(v) for k, v in (tokens or {}).items()}
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#163** · `low/maintainability` · `L83` · `partition(":")` treats everything before the first colon as a single kind, and the surrounding text is neithe…
  <details><summary>详情与建议</summary>

  `partition(":")` treats everything before the first colon as a single kind, and the surrounding text is neither trimmed nor split. A CSS-style grouped/compound selector (e.g. `widget.Button, widget.Label:hover`, or `"widget.Button "` with trailing whitespace) is therefore silently mis-compiled into one bogus attribute selector such as `QWidget[cairnClass="Button, widget.Label"]:hover`, which matches nothing. Either reject selectors containing `,`/whitespace explicitly or split and normalise them.

  **现有代码**

  ```
  83    base, _, state = selector.partition(":")
  ```

  **建议**

  ```
      parts = [p.strip() for p in selector.split(",")]
      compiled: list[str] = []
      for part in parts:
          base, _, state = part.partition(":")
          suffix = f":{state}" if state else ""
          if base.startswith("widget."):
              compiled.append(f'QWidget[cairnClass="{base[len("widget."):]}"]{suffix}')
          else:
              compiled.append(part)
      return ", ".join(compiled)
  ```

  </details>

## `src/ui_tools/page/page.py`

待办 1 / 共 2

- [x] **#166** · `medium/bug` · `L37-L39` · `Page` inherits `Node.clear()`, which empties `self._children`. ✅
  <details><summary>详情与建议</summary>

  `Page` inherits `Node.clear()`, which empties `self._children`. But since `Page.add` is delegated to the root layout, `self._children` holds exactly one thing: the root layout. So the public `page.clear()` does NOT clear the page's contents (the components live inside `self.layout._children`); it silently detaches the root layout, breaking the invariant established in `__init__` ("布局作为结构子节点，编译树才连得上"). After that, `page.add(...)` still succeeds against a layout that is no longer part of the tree, so the page compiles/renders empty with no error. Consider overriding `clear()` so it clears the layout's children (or re-attaches the layout).

  **现有代码**

  ```
  37    def add[T](self, component: T, *, at: object | None = None) -> T:
  38        """把部件放进根布局（`add` = 加持有）。"""
  39        return self.layout.add(component, at=at)
  ```

  **建议**

  ```
      def clear(self) -> None:
          """清空页面内容（保留根布局）。"""
          self.layout.clear()
  
      def add[T](self, component: T, *, at: object | None = None) -> T:
          """把部件放进根布局（`add` = 加持有）。"""
          return self.layout.add(component, at=at)
  ```

  </details>
- [ ] **#167** · `low/bug` · `L32-L35` · `set` destroys the current state (`self.clear()`) *before* the replacement layout is built.
  <details><summary>详情与建议</summary>

  `set` destroys the current state (`self.clear()`) *before* the replacement layout is built. If `layout(**opts)` raises — e.g. a `Layout` subclass requiring constructor arguments that `opts` does not supply — `self.layout` keeps pointing at the now-detached old layout and the new one is never added, leaving the page in an inconsistent state (attribute and structural child disagree). Build the new layout first, then swap it in.

  **现有代码**

  ```
  32        self.clear()
  33        self.layout = layout(**opts)
  34        super().add(self.layout)
  35        return self.layout
  ```

  **建议**

  ```
          new_layout = layout(**opts)
          self.clear()
          self.layout = new_layout
          super().add(new_layout)
          return self.layout
  ```

  </details>

# 模块 `app`

## `src/app/__init__.py`

待办 1 / 共 1

- [ ] **#1** · `low/documentation` · `L4-L7` · Module docstring states "只有 App 认识领域" (only `App` knows the domain), but the class that actually constructs th…
  <details><summary>详情与建议</summary>

  Module docstring states "只有 App 认识领域" (only `App` knows the domain), but the class that actually constructs the domain services here is `Feature`, and `App` (from `ui_tools.core`) is explicitly forbidden from importing `feature`/`core.storage` (see `tests/test_architecture.py`). The wording is therefore misleading — it suggests the UI `App` is the domain owner. Suggest naming `Feature` explicitly.

  **现有代码**

  ```
  4"""应用层：组合内核 / 领域 / `ui_tools`，按平台发布。
  5
  6**只有 App 认识领域**（构造域服务、静态挂载）；平台 UI 在子包（`win` / …）。
  7"""
  ```

  **建议**

  ```
  """应用层：组合内核 / 领域 / `ui_tools`，按平台发布。
  
  **只有应用层的 `Feature` 认识领域**（构造域服务、静态挂载）；平台 UI 在子包（`win` / …）。
  """
  ```

  </details>

## `src/app/__main__.py`

待办 1 / 共 1

- [ ] **#0** · `low/maintainability` · `L17` · `main()` is annotated `-> int`, yet the unsupported-platform branch raises `SystemExit` (with a string message…
  <details><summary>详情与建议</summary>

  `main()` is annotated `-> int`, yet the unsupported-platform branch raises `SystemExit` (with a string message) instead of returning an int. Any caller that imports and calls `main()` programmatically — e.g. a launcher or CI wrapper — will have the whole process terminated here rather than receiving an exit code, so the declared contract is only honored on one branch. Prefer returning a non-zero int and let the `if __name__ == "__main__"` guard convert it into the exit status (this also keeps message-to-stderr handling in one place).

  **现有代码**

  ```
  17    raise SystemExit(f"该平台暂无 UI：{sys.platform}")
  ```

  **建议**

  ```
      print(f"该平台暂无 UI：{sys.platform}", file=sys.stderr)
      return 2
  ```

  </details>

## `src/app/win/backend/__init__.py`

待办 1 / 共 1

- [ ] **#15** · `medium/bug` · `L61` · `updated` is coerced from the free-form `attrs` dict without validation, and `note_cards` builds every card in…
  <details><summary>详情与建议</summary>

  `updated` is coerced from the free-form `attrs` dict without validation, and `note_cards` builds every card in a single list comprehension with no per-note isolation, so one malformed value aborts the whole card list:
- `int(show.attrs.get("updated") or 0)` raises `ValueError` for a non-numeric string (e.g. imported/props data);
- `datetime.fromtimestamp(ms / 1000, tz=UTC)` raises `ValueError`/`OverflowError`/`OSError` for out-of-range values.
Additionally, future timestamps (clock skew / synced data) give negative elapsed seconds and are silently reported as "刚刚". Use a tolerant conversion helper, guard the datetime conversion, and treat non-positive elapsed time explicitly.

  **现有代码**

  ```
  61        meta=fmt_time(int(show.attrs.get("updated") or 0)),
  ```

  **建议**

  ```
          meta=fmt_time(_to_ms(show.attrs.get("updated"))),
  ```

  </details>

## `src/app/win/windows/app.py`

待办 4 / 共 4

- [ ] **#8** · `medium/bug` · `L37-L39` · The signal is mutated before `super().__init__()` runs.
  <details><summary>详情与建议</summary>

  The signal is mutated before `super().__init__()` runs. `app_theme()` (evaluated inside the `super().__init__(...)` call) does file I/O and can raise; if it (or `App.__init__`) fails, `vault.signal` is left pointing at a Feature bound to domains while the App itself was never constructed — any other holder of that signal (reuse of the vault after a failed open) silently talks to half-built wiring. Build the theme first, call `super().__init__()`, then publish the feature on the signal (the ordering is not required by the constructor: `Session(vault.signal)` does not depend on the assignment).

  **现有代码**

  ```
  37        feature = Feature(vault, vault.signal)
  38        vault.signal.feature = feature
  39        super().__init__(Session(vault.signal), theme=theme or app_theme())
  ```

  **建议**

  ```
          feature = Feature(vault, vault.signal)
          super().__init__(Session(vault.signal), theme=theme or app_theme())
          vault.signal.feature = feature
  ```

  </details>
- [ ] **#9** · `medium/bug` · `L62-L63` · Check-then-act on the filesystem: `path.exists()` is true for a regular file, for an empty directory, and for …
  <details><summary>详情与建议</summary>

  Check-then-act on the filesystem: `path.exists()` is true for a regular file, for an empty directory, and for a directory that a concurrent process creates between the check and the load. In the first two cases `Vault.load` raises `CairnError("not a valid bucket")` even though the user clearly meant "start here", and the race means the branch chosen may not match the state at load time. Prefer probing for the catalog file and falling back to create, or catching `CairnError` and creating (`Bucket.create` already refuses to clobber an existing catalog, so this is safe).

Also, `open()` acquires a `Bucket` (SQLite catalog connection) that no caller can release — there is no `close()`/context-manager on `CairnApp`.

  **现有代码**

  ```
  62        path = root or _default_root()
  63        vault = Vault.load(path) if path.exists() else Vault.create(path)
  ```

  **建议**

  ```
          path = root or _default_root()
          try:
              vault = Vault.load(path)
          except CairnError:
              vault = Vault.create(path)
  ```

  </details>
- [ ] **#10** · `low/maintainability` · `L29-L30` · The `CAIRN_VAULT`-less fallback resolves against the process working directory.
  <details><summary>详情与建议</summary>

  The `CAIRN_VAULT`-less fallback resolves against the process working directory. For a GUI entry point the cwd is whatever the shell/shortcut happened to start in (often `C:\Windows\System32`), so the vault silently lands in an unrelated — possibly unwritable (`Vault.create` -> `mkdir` raises `PermissionError`) — location, and two launches from different directories see two different vaults. Prefer a user-scoped path (e.g. under `%APPDATA%`/`Path.home()`) and read the env value with an emptiness check, since a set-but-empty `CAIRN_VAULT` currently yields `Path("") == "."`, which `exists()` reports as true and then fails to load.

  **现有代码**

  ```
  29def _default_root() -> Path:
  30    return Path(os.environ.get("CAIRN_VAULT", str(Path.cwd() / "vault")))
  ```

  **建议**

  _（无）_

  </details>
- [ ] **#11** · `low/maintainability` · `L40` · `self.feature` is written but never read anywhere in the repo (tests, tools and the shell all go through `vaul…
  <details><summary>详情与建议</summary>

  `self.feature` is written but never read anywhere in the repo (tests, tools and the shell all go through `vault.signal.feature`), so it is dead state on the instance. Either drop it or expose it deliberately via a documented accessor so it is clear it is part of the public surface.

  **现有代码**

  ```
  40        self.feature = feature
  ```

  **建议**

  _（无）_

  </details>

## `src/app/win/windows/notes.py`

待办 1 / 共 1

- [ ] **#55** · `low/maintainability` · `L48` · `self.nav` is handed to the App through `parts()`, but nothing ever binds its `clicked` signal: the only `clic…
  <details><summary>详情与建议</summary>

  `self.nav` is handed to the App through `parts()`, but nothing ever binds its `clicked` signal: the only `clicked` binding in the repo is `self.density.clicked`, `Facet.navigate()` is never called anywhere, and `App.add()` merely places the part into the `nav` slot (`Slot("nav", expects="nav")`). As written, the nav entry renders but clicking it does nothing, so the "导航条目" promised by the class docstring is currently inert. If navigation is intentionally deferred, please mark it (TODO/comment) so it is not mistaken for dead UI; otherwise wire it here, e.g. `self.bind.add(self.nav.clicked, ...)` pointing at the page/route it should open.

  **现有代码**

  ```
  48        self.nav = Button("\u7b14\u8bb0", name="nav_note")
  ```

  **建议**

  _（无）_

  </details>

## `src/app/win/windows/theme.py`

待办 0 / 共 4

- [x] **#4** · `high/bug` · `L60-L63` · The module docstring promises a fallback so the app can "always start", but only a *missing* file is guarded. ✅
  <details><summary>详情与建议</summary>

  The module docstring promises a fallback so the app can "always start", but only a *missing* file is guarded. If the JSON is malformed/truncated, `load_theme()` raises `json.JSONDecodeError` (and an unreadable file raises `OSError`), so startup crashes. There is also a TOCTOU window: the file can disappear between `path.exists()` and the read inside `load_theme()`. Wrap the load itself instead of the existence check.

  **现有代码**

  ```
  60    path = theme_dir() / f"{name}.json"
  61    if path.exists():
  62        return load_theme(path)
  63    return Theme(_FALLBACK_TOKENS, _FALLBACK_STYLES)
  ```

  **建议**

  ```
      path = theme_dir() / f"{name}.json"
      try:
          return load_theme(path)
      except (OSError, ValueError) as err:  # json.JSONDecodeError is a ValueError
          logger.warning("主题 %s 加载失败，退回内置默认：%s", path, err)
          return Theme(_FALLBACK_TOKENS, _FALLBACK_STYLES)
  ```

  </details>
- [x] **#6** · `medium/bug` · `L55` · `parents[4]` hard-codes the source layout (`src/app/win/windows/theme.py`). ✅
  <details><summary>详情与建议</summary>

  `parents[4]` hard-codes the source layout (`src/app/win/windows/theme.py`). If this module is ever packaged/vendored/zipped at a shallower depth, `Path.parents[4]` raises `IndexError`, which directly breaks the "always start" guarantee of this module. Search upwards for the `config/theme` directory (or fall back to a bounded index) instead of relying on a fixed depth.

  **现有代码**

  ```
  55    return Path(__file__).resolve().parents[4] / "config" / "theme"
  ```

  **建议**

  ```
      here = Path(__file__).resolve()
      for parent in here.parents:
          candidate = parent / "config" / "theme"
          if candidate.is_dir():
              return candidate
      return here.parent / "config" / "theme"
  ```

  </details>
- [x] **#5** · `medium/security` · `L60` · `name` is interpolated straight into the path. ✅
  <details><summary>详情与建议</summary>

  `name` is interpolated straight into the path. Any caller passing a name containing path separators or `..` (e.g. `"../../etc/passwd"`) escapes the theme directory and can load an arbitrary JSON file from disk; it also silently accepts a name that does not correspond to a real theme. Validate that `name` is a bare file stem before joining.

  **现有代码**

  ```
  60    path = theme_dir() / f"{name}.json"
  ```

  **建议**

  ```
      if not name or Path(name).name != name or name in {".", ".."}:
          logger.warning("非法主题名 %r，退回内置默认", name)
          return Theme(_FALLBACK_TOKENS, _FALLBACK_STYLES)
      path = theme_dir() / f"{name}.json"
  ```

  </details>
- [x] **#7** · `low/maintainability` · `L52-L54` · A `CAIRN_THEME_DIR` override is trusted unconditionally. ✅
  <details><summary>详情与建议</summary>

  A `CAIRN_THEME_DIR` override is trusted unconditionally. If it points at a file or a non-existent path, every theme lookup silently fails `exists()` and the app quietly runs on the built-in defaults, masking a misconfigured deployment with no diagnostic. Consider validating it is a directory and warning otherwise.

  **现有代码**

  ```
  52    override = os.environ.get("CAIRN_THEME_DIR")
  53    if override:
  54        return Path(override)
  ```

  **建议**

  ```
      override = os.environ.get("CAIRN_THEME_DIR")
      if override:
          path = Path(override)
          if path.is_dir():
              return path
          logger.warning("CAIRN_THEME_DIR=%s 不是目录，忽略该覆盖", override)
  ```

  </details>
