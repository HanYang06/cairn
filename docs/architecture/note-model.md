# 笔记模型（Note Model）

> L3 领域概念规格。回答"笔记到底是什么"，并定死节点 / 基板 / 关系 / 组装四要素。
> 承接内核 [`storage.md`](./storage.md)、[`domains.md`](./domains.md)、[`kernel.md`](./kernel.md)。

状态：**草案 v0.1**（概念定稿讨论中，未写代码）

---

## 1. 一句话定义

> **笔记是思考的最小可移动单元，写给未来的自己与相邻的念头。**
> 内容在**基板**里，意义在**关系**里；它是**超文本**，不是文档。

| | 笔记 | 文档（Word） | 文本 |
|---|---|---|---|
| 目的 | 捕捉思考 | 交付成品 | 承载字节 |
| 结构 | 涌现（链接长出来） | 预设（章节分页） | 线性 |
| 生命周期 | 永不完结 | 会完成/发布 | 无身份 |
| 关系 | 对其它笔记（图） | 对读者（呈现） | 对介质 |

**单条笔记没价值，笔记之间的图才是资产。**

---

## 2. 四要素

```
Node      节点   —— 可寻址的最小单位（= 一个对象 / OID）
Substrate 基板   —— 节点里装的东西：无模式的多模态片段序列
Relation  关系   —— 节点之间的有向边，一等对象、带署名
Composition 组装 —— 一种节点：其内容是"对其它节点的排布/引用" → 文档/博客
```

- **没有标题**：标题是显示时算出来的（首行 / 摘要），不是字段。
- **没有正文**："正文"就是基板，无固定模式。
- **没有链接**：链接是**独立的关系对象**，不是正文里的语法糖；`[[ ]]` 只是创建关系的一种输入方式。

---

## 3. 身份与全局唯一编码

- 节点身份 = `(author_pubkey, oid)`：**OID 全局唯一 + 作者公钥绑定**，密码学保证。
- 作者绑定：身份 = 一对密钥（Ed25519 签名 + X25519 协商），私钥由主密钥封装存于本库。
- 未来若要绑定"真实用户"，靠身份可携带、可声明（`profile` 节点）实现，不在底层强加。
- **不可篡改性由内容寻址保证**：一个 OID 对应的字节，任何人改不了。

---

## 4. 两种演化模式（关键）

笔记被改变分三类，规则不同：

### 4.1 作者自改（同一节点）

- 原件**必然变更**（head 前进）。
- **保留"一点点版本路径"，不做 git 式全历史**：默认保留**最近 30 天时间窗**内的版本，超出即由 GC 裁剪（`vault.gc(retention_ms=...)` 可调）。
- 对应内核 `seq / prev / author` 版本链；当前实现是"每版都归档"，**需要加保留策略**。

### 4.2 协作修改（同一节点，多人）

- 类似 WPS 共同协作：多人对**同一节点**演进。
- 需要**多作者版本链** + 并发合并。
- **诚实边界**：真正的实时协作（CRDT/OT、冲突合并）是独立难题，**本阶段不做**；模型预留多作者署名，实现后置。

### 4.3 再发布 / 派生（新节点，跨作者）

- 类同开源：基于他人内容升级、引用、改写。
- **生成新节点 + `derived-from` 关系边**，署名派生者。
- **原件不受任何影响**（原件在原作者的库里，别人改不到）。
- 这正是**笔记族谱**的来源。

---

## 5. 笔记族谱（Provenance DAG）

- 跨作者改写链（张 → 李 → 明 → 红）构成一张**派生 DAG**，与 git commit 图 / 学术引用图同构。
- 每个节点、每条关系**都带作者、都签名**。
- **贡献者** = 沿 DAG 回溯的作者集合。
- **原作者回溯** = 查 `to = 本节点, kind = derived-from` 的传递闭包 → "谁基于我改过"。
- **阅览是隐私**：默认不追踪阅读；读者可**自愿**发 `read` / `endorse` 边，否则无从得知。

---

## 6. 基板（Substrate）：多模态片段序列

```
substrate = [ fragment, ... ]
fragment  ∈ 文字 | 对象引用 | 矢量图形 | 组合引用 | ...
```

- **文字**：结构化文本片段（非 Markdown，带语义，可嵌关系锚点）。
- **图像 / 视频 / 声音 / 任意文件**：以**对象引用（OID）**嵌入，二进制本体另存为对象，不内联。
- **图形 / 手绘**：**矢量坐标**，与 SVG 同族但更轻（存图元，不逐点写死）。
- **组合引用（transclusion）**：在基板里嵌入另一个节点 → 组装的基础。

### 6.1 手绘与形状识别（重要决策）

- **不保存原始笔迹**：数据量不可控（"有人一直画"会爆）。
- **只保存识别后的标准形状**（矢量坐标数据）。
- 因此**识别质量是核心**：画得歪扭时，系统要"标准化成它想表达的形状"，而不是"照原样存歪扭"。
- 用户不满意 → **撤销重画**即可，不必重建。
- 识别引擎可为设备端小模型（见 §8），输出标准图元写回基板。

---

## 7. 组装 / 投影（Composition）

- **组装** = 一种节点：基板是"对其它节点的排布 / 引用"。
- **文档 = 组装**；**博客 = 公开档里的组装**；渲染是投影。
- 于是"写文档"与"发博客"共用同一模型，只是**可见性档位不同**（私密 / 公共 / 公开）。
- 笔记是原料，文档/博客是投影——直通 P2P 北极星。

---

## 8. Agent 与 AI

### 8.1 立场

- **AI 不是作者身份，而是一种"使用方式"**：用户通过 AI 调用能力。
- AI 的动作**归因于发起它的用户**（可标注"AI 辅助 / 用了哪个模型"）。
- 因此**不为 AI 单做一套 API**：把内核能力暴露为**可被调用的工具**（MCP / skill），AI 与人都走同一套。

### 8.2 内核能力即工具集

读（`open/iter`）、订阅（`subscribe`）、写（`put`）、建关系——天然就是一批"工具"。AI 接入 = 挂一个 Agent，按需调用这些工具并写回结果。

### 8.3 低能力 AI 与高能力 AI

| | 低能力 AI | 高能力 AI |
|---|---|---|
| 形态 | 自研机器人 / 规则引擎 | 大模型 Agent |
| 用途 | **规则性治理**：用户组合规则形成社区规则 | 帮助写作、整理、复杂治理 |
| 成本 | 低、敏捷 | 看谁有钱 |

- 社区治理是**未来 P2P 社区**的一部分，底层只提供"工具 + 关系"，治理逻辑属上层。

### 8.4 提案（proposal）

- Agent 可先产出**提案**（不落盘），人接受后才成为派生 / 关系。
- 保证"人在环上"，AI 不越权。

---

## 9. 捕捉面 vs 编辑面（及时性）

念头会过期，所以**捕捉必须零摩擦**：

| | 捕捉面 catch | 编辑面 develop |
|---|---|---|
| 时刻 | 念头一闪 | 坐下来整理 |
| 要求 | 秒开、零成本 | 富、慢一点无妨 |
| 技术 | **原生 Qt、常驻热键** | **Web 富编辑 / 画布**（可预加载） |

- 总体架构：**Qt 外壳（窗口/列表/搜索/热键捕捉/时间轴）+ Web 富编辑画布**，两者分离。
- 首期入口：**从文档（组装）角度切入**——先能"写文档"，再补"闪现捕捉"。

---

## 10. 与内核的映射

| 概念 | 内核落点 |
|---|---|
| 节点 | 对象（`type = cairn.note`），`author` = 身份公钥 |
| 基板 | 对象 payload（CBOR 片段序列） |
| 关系 | 独立对象（`type = cairn.relation`）：`from / to / kind / author / sig` |
| 组装 | 对象（`type = cairn.composition`），基板为节点引用排布 |
| 版本路径 | manifest `seq / prev / author` + 保留策略 |
| 族谱 | 索引对关系图做闭包计算 |

关系类型（草案）：`derived-from`、`edits`、`annotates`、`replies-to`、`references`、`transcludes`、`read`、`endorse`。

---

## 11. 待定问题

1. 版本保留策略：**已定——默认 30 天时间窗**，GC 裁剪；窗口可配置。
2. 协作修改（同节点多作者 + 合并）是否真做、何时做。
3. 身份与"真实用户"的绑定的具体机制（可携带、可声明）。
4. 关系对象的数量增长与打包优化。
5. 形状识别的引擎与精度标准（设备端小模型 vs 云端）。
6. 低能力 AI 的规则表达格式与社区治理接口。
7. "读"的自愿记录如何防滥用（隐私边界）。
